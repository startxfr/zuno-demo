---
# Cluster Observability operator install; the MonitoringStack instance is a
# separate Application (application-d1.yaml) applied after this one is
# healthy.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zuno-mesh-monitoring-d0
  namespace: openshift-gitops
  annotations:
    # Sync-wave order only matters for the root App-of-Apps path; Ansible
    # ignores it.
    argocd.argoproj.io/sync-wave: "-164"
spec:
  project: zuno
  source:
    repoURL: https://github.com/startxfr/zuno-demo.git
    targetRevision: main
    path: gitops/charts/mesh-monitoring
    helm:
      releaseName: zuno-mesh-monitoring-d0
      values: |
        project:
          enabled: true
        operator:
          enabled: true
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-cluster-observability-operator
  ignoreDifferences:
    # OLM rewrites this annotation at runtime; without this, ArgoCD sees
    # permanent drift and fights it.
    - group: operators.coreos.com
      kind: OperatorGroup
      jsonPointers:
        - /metadata/annotations/olm.providedAPIs
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      # Namespace isn't created elsewhere, so CreateNamespace is required here.
      - CreateNamespace=true
