---
# Node Feature Discovery operator install; must sync before nvidia_gpu. The
# NodeFeatureDiscovery CR operand is a separate Application
# (application-d1.yaml).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zuno-nfd-d0
  namespace: openshift-gitops
  annotations:
    # Sync-wave only matters for the root App-of-Apps path; Ansible orders `nfd`
    # before `nvidia_gpu` in day0_install.yml instead.
    argocd.argoproj.io/sync-wave: "-204"
spec:
  project: zuno
  source:
    repoURL: https://github.com/startxfr/zuno-demo.git
    targetRevision: main
    path: gitops/charts/nfd
    helm:
      releaseName: zuno-nfd-d0
      values: |
        cluster-nfd:
          project:
            enabled: true
          operator:
            enabled: true
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-nfd
  ignoreDifferences:
    # OLM rewrites this annotation at runtime; ignored to avoid permanent ArgoCD drift.
    - group: operators.coreos.com
      kind: OperatorGroup
      jsonPointers:
        - /metadata/annotations/olm.providedAPIs
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true # namespace isn't created elsewhere
