---
# No Day0 content for diagram-render; see gitops/apps/README.md's -d0/-d1
# convention (same pattern as gitops/apps/mcp-confluence - a stateless
# rendering service has no schema/prerequisite Job to run before its own
# Deployment).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zuno-diagram-render-d0
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "-138"
spec:
  project: zuno
  source:
    repoURL: https://github.com/startxfr/zuno-demo.git
    targetRevision: main
    path: gitops/charts/noop
    helm:
      releaseName: zuno-diagram-render-d0
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-gitops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
