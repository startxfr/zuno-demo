apiVersion: v1
kind: Service
metadata:
  name: sales-db-mcp
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: sales-db-mcp
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: sales-db-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
