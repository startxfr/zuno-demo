apiVersion: v1
kind: Service
metadata:
  name: confluence-mcp
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: confluence-mcp
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: confluence-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
