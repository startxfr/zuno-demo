apiVersion: v2
name: ai-gateway
description: >-
  Zuno AI Inference Gateway (ADR-0009): resolves classification-eligible
  providers (ADR-0021) and tries them in fallback order (ADR-0020) behind
  an OpenAI-compatible API, kept separate from components/agent-runtime's
  orchestration. Image built from components/ai-gateway. See docs/adr/0009,
  0020, 0021.
type: application
version: 0.1.0
appVersion: "0.1.0"
