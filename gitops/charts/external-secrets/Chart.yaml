apiVersion: v2
name: external-secrets
description: Deploys the External Secrets Operator and the Vault-backed ClusterSecretStore used platform-wide.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: operator
    version: "21.3.277"
    repository: "alias:startx"
