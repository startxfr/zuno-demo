apiVersion: v1
kind: Service
metadata:
  name: aap-mcp
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: aap-mcp
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: aap-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
