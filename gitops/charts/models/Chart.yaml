apiVersion: v2
name: models
description: KServe ServingRuntime and InferenceService serving Qwen2.5-7B-Instruct via vLLM behind an OpenAI-compatible API.
type: application
version: 0.1.0
appVersion: "0.1.0"
