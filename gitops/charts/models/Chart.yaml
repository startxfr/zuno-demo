apiVersion: v2
name: models
description: KServe ServingRuntime and InferenceService serving Qwen3.6-27B (FP8) via vLLM behind an OpenAI-compatible API.
type: application
version: 0.1.0
appVersion: "0.1.0"
