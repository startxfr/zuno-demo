apiVersion: v2
name: finage
description: >-
  Finage frontend (portal + chat UI) and BFF: plain Kubernetes
  Deployment/Service/Route (frontend) and Deployment/Service (BFF) in the
  shared zuno-ai-run namespace.
type: application
version: 0.1.0
appVersion: "0.1.0"
