# Shared by two ArgoCD Applications: operator (zuno-kiali-d0) and instance
# (zuno-kiali-d1). Both default false so `helm template`/`helm lint` render
# nothing.
operator:
  enabled: false
kiali:
  enabled: false

meshNamespace: zuno-mesh
