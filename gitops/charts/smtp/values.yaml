# Shared by two ArgoCD Applications: namespace (zuno-smtp-d0) and
# credentials (zuno-smtp-d1). Both default false so `helm template`/`helm
# lint` render nothing.
namespace:
  enabled: false
credentials:
  enabled: false
