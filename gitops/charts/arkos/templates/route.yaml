# Fixed hostname (arkos.routeHost helper): the OIDC redirect URI and
# Keycloak client's registered redirect URI both depend on it.
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: arkos-frontend
  namespace: {{ .Values.namespace }}
  labels:
    zuno.io/agent: arkos
    app.kubernetes.io/component: frontend
  annotations:
    argocd.argoproj.io/sync-wave: "105"
spec:
  host: {{ include "arkos.routeHost" . }}
  to:
    kind: Service
    name: arkos-frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
