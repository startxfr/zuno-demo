apiVersion: v2
name: observability
description: >-
  OTLP trace/metric collection path: the Red Hat OpenTelemetry operator and
  the shared OpenTelemetryCollector instance every service sends OTLP to.
type: application
version: 0.1.0
