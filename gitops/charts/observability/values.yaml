# Shared by two ArgoCD Applications: operator (zuno-observability-d0) and
# collector (zuno-observability-d1). Both default false so `helm
# template`/`helm lint` render nothing.
operator:
  enabled: false
collector:
  enabled: false
