apiVersion: v2
name: openshift-ai
description: >-
  Red Hat OpenShift AI operator (OLM Subscription, channel discovered at
  Ansible run time) and the DataScienceCluster with kserve (RawDeployment)
  enabled. Wraps the startx cluster-ods chart as a dependency.
type: application
version: 0.3.0
appVersion: "0.3.0"
dependencies:
  - name: cluster-ods
    version: "21.3.277"
    repository: "alias:startx"
