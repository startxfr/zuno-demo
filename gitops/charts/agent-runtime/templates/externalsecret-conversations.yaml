# ADR-0212: resolves the same zuno/agent-runtime/conversations-postgresql-app
# Vault credential gitops/charts/postgresql/templates/externalsecret-conversations.yaml
# also resolves - that Secret lives in zuno-data, agent-runtime's pods live
# in zuno-ai-run, and secretKeyRef can't cross namespaces (same pattern as
# templates/externalsecret-checkpoint.yaml in this chart).
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.conversationsDb.credentials.secretName }}
  namespace: {{ .Values.namespace }}
  labels:
    zuno.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "70"
spec:
  refreshInterval: {{ .Values.conversationsDb.credentials.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.conversationsDb.credentials.secretStoreName }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.conversationsDb.credentials.secretName }}
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: {{ .Values.conversationsDb.credentials.vaultPath }}
        property: username
    - secretKey: password
      remoteRef:
        key: {{ .Values.conversationsDb.credentials.vaultPath }}
        property: password
