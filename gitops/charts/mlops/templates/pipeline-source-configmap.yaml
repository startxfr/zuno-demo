apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mlops.fullname" . }}-pipeline-source
  namespace: {{ .Values.platform.namespace }}
  labels:
    {{- include "mlops.labels" . | nindent 4 }}
data:
  pipeline.py: |
{{ tpl (.Files.Get "files/pipeline.py.tpl") . | indent 4 }}
