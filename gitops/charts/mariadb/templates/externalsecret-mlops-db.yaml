{{- if .Values.mariadbOperand.enabled }}
# Syncs the pre-seeded Vault zuno/mlops/dspa-mysql credential (self-generated
# by ansible/roles/vault/tasks/install.yml, an internal service credential -
# not operator-supplied via ansible/confidential.yml) into the Secret the
# User CR's passwordSecretKeyRef expects (templates/database-mlops.yaml).
# Vault path matches gitops/charts/mlops/values.yaml's own
# pipelineServer.metadataDatabase.externalMySQL.externalSecret.remoteKey.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mariadb-mlops-db-password
  namespace: {{ .Release.Namespace }}
  labels:
    zuno.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-35"
spec:
  refreshInterval: {{ .Values.credentials.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.credentials.secretStoreName }}
    kind: ClusterSecretStore
  target:
    name: mariadb-mlops-db-password
  data:
    - secretKey: password
      remoteRef:
        key: {{ .Values.mlopsDatabase.vaultPath }}
        property: password
{{- end }}
