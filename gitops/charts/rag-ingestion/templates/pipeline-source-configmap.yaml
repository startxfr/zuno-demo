apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rag-ingestion.fullname" . }}-pipeline-source
  namespace: {{ .Values.platform.namespace }}
  labels:
    {{- include "rag-ingestion.labels" . | nindent 4 }}
data:
  pipeline.py: |
{{ tpl (.Files.Get "files/pipeline.py.tpl") . | indent 4 }}
