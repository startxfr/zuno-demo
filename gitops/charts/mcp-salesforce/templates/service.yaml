apiVersion: v1
kind: Service
metadata:
  name: salesforce-mcp
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: salesforce-mcp
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: salesforce-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
