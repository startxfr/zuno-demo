apiVersion: v2
name: keycloak
description: >-
  Deploys the Keycloak CR (Red Hat build of Keycloak), Google Workspace IdP
  federation, and the `zuno` realm via a KeycloakRealmImport.
type: application
version: 0.2.0
appVersion: "n/a"
dependencies:
  - name: operator
    version: "21.3.277"
    repository: "alias:startx"
