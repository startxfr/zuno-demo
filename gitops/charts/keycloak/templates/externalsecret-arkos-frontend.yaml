{{- if .Values.keycloak.enabled }}
# ADR-0326 (WP-31): arkos-frontend is now a confidential OIDC client
# (server-side auth-code exchange), same shape as tekos-frontend - see
# that client's own realm-zuno.json entry and
# externalsecret-tekos-frontend.yaml for why. The client secret is
# vault-generated and injected via KC_VAULT (templates/keycloak.yaml).
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: arkos-frontend-client-secret
  namespace: {{ .Values.namespace }}
  labels:
    zuno.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: arkos-frontend-client-secret
    creationPolicy: Owner
  data:
    - secretKey: client_secret
      remoteRef:
        key: keycloak/arkos-frontend
        property: client_secret
{{- end }}
