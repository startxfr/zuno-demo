# ExternalSecret resolving the Keycloak bootstrap admin credentials that the
# vault role auto-generates at secret/zuno/keycloak/admin (see
# ansible/roles/vault/tasks/configure.yml). Consumed by the Keycloak CR's
# spec.bootstrapAdmin.user.secret (templates/keycloak.yaml). ADR-0024: no
# secret is ever hardcoded here.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.adminSecretName }}
  namespace: {{ .Values.namespace }}
  labels:
    zuno.io/managed-by: argocd
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ .Values.adminSecretName }}
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: zuno/keycloak/admin
        property: username
    - secretKey: password
      remoteRef:
        key: zuno/keycloak/admin
        property: password
