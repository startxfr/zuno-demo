apiVersion: v2
name: nvidia-gpu
description: >-
  NVIDIA GPU Operator (OLM Subscription, certified-operators catalog) and
  its ClusterPolicy, enabling GPU scheduling. Wraps the startx cluster-gpu
  chart as a dependency.
type: application
version: 0.2.0
appVersion: "0.2.0"
dependencies:
  - name: cluster-gpu
    version: "21.3.277"
    repository: "alias:startx"
