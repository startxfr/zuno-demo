apiVersion: v2
name: perses
description: >-
  ADR-0551/WP-138: namespace-scoped Perses dashboards duplicating
  zuno-monitoring's Grafana dashboard set, on top of the Cluster
  Observability Operator's Perses CRDs (already installed cluster-wide by
  gitops/charts/mesh-monitoring's application-d0.yaml - no operator
  Subscription of its own here).
type: application
version: 0.1.0
