apiVersion: v2
name: perses
description: >-
  ADR-0551/ADR-0552/WP-138: namespace-scoped Perses dashboards duplicating
  zuno-monitoring's Grafana dashboard set, targeting RHOAI's existing
  data-science-perses instance (ADR-0552) rather than running an
  independent one - an earlier attempt at the latter broke RHOAI's own
  dashboards live, see ADR-0551's dated correction note. No operator
  Subscription or server CR of its own here.
type: application
version: 0.1.0
