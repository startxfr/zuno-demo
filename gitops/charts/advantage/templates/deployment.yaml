apiVersion: apps/v1
kind: Deployment
metadata:
  name: advantage-frontend
  namespace: {{ .Values.namespace }}
  labels:
    zuno.io/agent: advantage
    app.kubernetes.io/component: frontend
  annotations:
    argocd.argoproj.io/sync-wave: "90"
spec:
  replicas: {{ .Values.frontend.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: frontend
      zuno.io/agent: advantage
  template:
    metadata:
      labels:
        app.kubernetes.io/component: frontend
        zuno.io/agent: advantage
    spec:
      serviceAccountName: advantage-frontend
      priorityClassName: zuno-workload-default
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: frontend
          image: "{{ include "advantage.frontendImage" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ACTIVE_AGENT
              value: {{ .Values.frontend.activeAgent | quote }}
            - name: KEYCLOAK_ISSUER_URL
              value: {{ include "advantage.keycloakIssuerUrl" . | quote }}
            {{- if .Values.keycloak.caConfigMapName }}
            - name: KEYCLOAK_CA_CERT_PATH
              value: /etc/pki/agent-frontend/keycloak-ca/ca.crt
            {{- end }}
            - name: OIDC_CLIENT_ID
              value: {{ .Values.frontend.oidcClientId | quote }}
            - name: SELF_BASE_URL
              value: {{ include "advantage.selfBaseUrl" . | quote }}
            - name: BFF_BASE_URL
              value: "http://advantage-bff.{{ .Values.namespace }}.svc.cluster.local:8080"
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: advantage-frontend-secrets
                  key: OIDC_CLIENT_SECRET
            - name: SESSION_HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: advantage-frontend-secrets
                  key: SESSION_HMAC_SECRET
            - name: SESSION_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: advantage-frontend-secrets
                  key: SESSION_ENCRYPTION_KEY
            - name: SESSION_MAX_LIFETIME_SECONDS
              value: {{ .Values.redis.sessionMaxLifetimeSeconds | quote }}
            - name: REDIS_ADDR
              value: {{ .Values.redis.addr | quote }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: advantage-frontend-secrets
                  key: REDIS_PASSWORD
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            {{- if .Values.keycloak.caConfigMapName }}
            - name: keycloak-ca
              mountPath: /etc/pki/agent-frontend/keycloak-ca
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if .Values.keycloak.caConfigMapName }}
        - name: keycloak-ca
          configMap:
            name: {{ .Values.keycloak.caConfigMapName }}
        {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: advantage-bff
  namespace: {{ .Values.namespace }}
  labels:
    zuno.io/agent: advantage
    app.kubernetes.io/component: bff
  annotations:
    argocd.argoproj.io/sync-wave: "90"
spec:
  replicas: {{ .Values.bff.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: bff
      zuno.io/agent: advantage
  template:
    metadata:
      labels:
        app.kubernetes.io/component: bff
        zuno.io/agent: advantage
    spec:
      serviceAccountName: advantage-bff
      priorityClassName: zuno-workload-default
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: bff
          image: "{{ include "advantage.bffImage" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: AGENT_NAME
              value: {{ .Values.bff.agentName | quote }}
            - name: KEYCLOAK_ISSUER_URL
              value: {{ include "advantage.keycloakIssuerUrl" . | quote }}
            - name: OIDC_AUDIENCE
              value: {{ .Values.bff.oidcAudience | quote }}
            - name: AGENT_RUNTIME_BASE_URL
              value: {{ .Values.bff.agentRuntimeBaseUrl | quote }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.bff.resources | nindent 12 }}
      volumes:
        - name: tmp
          emptyDir: {}
