apiVersion: v1
kind: Service
metadata:
  name: diagram-render
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: diagram-render
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: diagram-render
  ports:
    - name: http
      port: 8080
      targetPort: http
