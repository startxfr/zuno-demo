apiVersion: v2
name: namespaces
description: Creates the five agent namespaces, one per agent instance regardless of whether it's actively deployed.
type: application
version: 0.1.0
appVersion: "0.1.0"
