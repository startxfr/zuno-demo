apiVersion: v1
kind: Service
metadata:
  name: git-forge-mcp
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: git-forge-mcp
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  selector:
    app.kubernetes.io/name: git-forge-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
