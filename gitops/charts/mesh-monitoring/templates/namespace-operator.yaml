{{- if .Values.operator.enabled }}
# PreSync hook: guarantees the namespace exists before the
# OperatorGroup/Subscription below.
apiVersion: v1
kind: Namespace
metadata:
  name: openshift-cluster-observability-operator
  labels:
    zuno.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/hook: PreSync
    openshift.io/description: "Red Hat Cluster Observability operator subscription, installed by Zuno."
    openshift.io/display-name: "Zuno Cluster Observability Operator"
    openshift.io/requester: zuno
{{- end }}
