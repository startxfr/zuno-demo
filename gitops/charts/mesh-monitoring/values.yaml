# Shared by two ArgoCD Applications: operator (zuno-mesh-monitoring-d0) and
# instance (zuno-mesh-monitoring-d1). Both default false so `helm
# template`/`helm lint` render nothing.
operator:
  enabled: false
monitoring:
  enabled: false

meshNamespace: zuno-mesh
