fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
PyJWT[crypto]==2.9.0
httpx==0.27.2
PyYAML==6.0.2
langchain-core==0.3.15
langchain-openai==0.2.4
# ADR-0415: app/image_providers.py calls the `openai` SDK's Images API
# directly (no LangChain wrapper exists for image generation) - already a
# transitive dependency of langchain-openai above, pinned explicitly here
# since this is now a direct import, not just a transitive one.
openai==1.54.4
langchain-anthropic==0.2.3
langchain-google-genai==2.0.4
langchain-mistralai==0.2.1
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0
# ADR-0104: semantic cache backend (Redis) and the embedding-service call
# app/semantic_cache.py makes for its bucketing key.
redis==8.1.0
