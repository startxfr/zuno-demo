# Ansible Automation

Ansible implements the infrastructure/application operations behind the public Make interface. The first repository stage provides validated dispatch skeletons; concrete component roles are implemented incrementally.
