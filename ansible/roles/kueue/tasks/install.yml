---
# Ordered alongside connectivity_link/lws/custom_metrics_autoscaler/jobset
# and ahead of openshift_ai - ADR-0321 installs this operator before the
# DataScienceCluster reconciles `spec.components.kueue.managementState:
# Unmanaged`.

# Package name confirmed against a live cluster's redhat-operators catalog
# as "kueue-operator"; still discover catalog/channel from the
# PackageManifest rather than hardcoding them, same pattern as
# ansible/roles/jobset/tasks/install.yml.
- name: "kueue | resolve the operator package"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/resolve_operator_package.yml"
  vars:
    operator_package_name: "{{ kueue_package_name | default('kueue-operator') }}"
    operator_subscription_namespace: "openshift-kueue-operator"
    operator_package_catalog_hint: "kueue"

- name: "kueue | apply the kueue GitOps Application (d0: operator)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/apply_gitops_app.yml"
  when: not (operator_package_skip_d0 | bool)
  vars:
    gitops_component_name: "kueue"
    gitops_app_phase: "d0"
    gitops_app_name: "zuno-kueue-d0"
    gitops_app_extra_helm_values:
      # project.enabled: true must be repeated here even though
      # application-d0.yaml already sets it: apply_gitops_app.yml's
      # gitops_app_extra_helm_values REPLACES spec.source.helm.values
      # wholesale rather than merging into it, so omitting this silently
      # dropped the Namespace-creating project subchart from the actual
      # applied Application, leaving openshift-kueue-operator created only
      # by ArgoCD's bare CreateNamespace=true fallback (no labels at all,
      # not even openshift.io/cluster-monitoring).
      project:
        enabled: true
      operator:
        enabled: true
        subscription:
          operator:
            channel: "{{ operator_package_channel }}"
            source:
              name: "{{ operator_package_catalog }}"
    gitops_app_wait_retries: 60
    gitops_app_wait_delay: 15

# Unlike jobset (no operand CR at all), Kueue needs its singleton `Kueue`
# CR plus the default ResourceFlavor/ClusterQueue/LocalQueue applied
# before it's actually usable - see gitops/charts/kueue's Chart.yaml.
- name: "kueue | apply the kueue GitOps Application (d1: operand CR + queue resources)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/apply_gitops_app.yml"
  vars:
    gitops_component_name: "kueue"
    gitops_app_phase: "d1"
    gitops_app_name: "zuno-kueue-d1"
    gitops_app_extra_helm_values:
      kueueOperand:
        enabled: true
      queueResources:
        enabled: true
    gitops_app_wait_retries: 30
    gitops_app_wait_delay: 15
