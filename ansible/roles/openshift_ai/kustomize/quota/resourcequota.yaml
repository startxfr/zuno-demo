apiVersion: v1
kind: ResourceQuota
metadata:
  name: zuno-ai-run-gpu-cap
  namespace: zuno-ai-run
  labels:
    zuno.io/managed-by: zuno-ansible
spec:
  hard:
    requests.nvidia.com/gpu: "1"
    limits.nvidia.com/gpu: "1"
