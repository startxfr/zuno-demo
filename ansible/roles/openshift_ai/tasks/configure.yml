---
# No configure phase: the GPU ResourceQuota for zuno-ai-run now lives in
# gitops/charts/namespaces (ADR-0312, folded in alongside the RHOAI
# dashboard label - see that chart's `openshiftAi` values block), not
# here. Nothing left for openshift_ai to configure beyond what
# install.yml already applies (same convention as
# ansible/roles/nfd/tasks/configure.yml and friends).
- name: "openshift_ai | nothing to configure - GPU quota moved to gitops/charts/namespaces (ADR-0312)"
  ansible.builtin.debug:
    msg: "openshift_ai has no configure phase - see gitops/charts/namespaces' openshiftAi values block."
