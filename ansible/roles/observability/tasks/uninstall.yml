---
# Reverse of install.yml: delete -d1 first (avoids ArgoCD fighting a CR
# deletion), then remove_operator.yml (needs the Subscription to still
# exist) before -d0's own delete.
- name: "observability | delete the observability GitOps Application (d1: shared OTLP collector)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/delete_gitops_app.yml"
  vars:
    gitops_app_name: "zuno-observability-d1"

- name: "observability | remove the operator (its owned CRDs - cascading the OpenTelemetryCollector instance - then the CSV, then the Subscription)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/remove_operator.yml"
  vars:
    operator_subscription_name: opentelemetry-product
    operator_namespace: openshift-opentelemetry-operator

- name: "observability | delete the observability GitOps Application (d0: cascades the remaining OperatorGroup/Namespace)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/delete_gitops_app.yml"
  vars:
    gitops_app_name: "zuno-observability-d0"
