# Ansible role: nvidia_gpu

This role owns precheck, preparation and/or configuration tasks for the **nvidia_gpu** scope. Current files are an implementation scaffold; executable tasks are expanded incrementally as the MVP is built.
