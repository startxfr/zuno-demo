---
# No configure phase: the ClusterPolicy applied in prepare.yml is the whole
# of what "configuring" the GPU Operator means for this demo - nothing
# workload-specific to layer on top.
- name: "nvidia_gpu | nothing to configure beyond the ClusterPolicy"
  ansible.builtin.debug:
    msg: "nvidia_gpu has no configure phase - see tasks/prepare.yml."
