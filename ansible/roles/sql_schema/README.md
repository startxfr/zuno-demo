# Ansible role: sql_schema

This role owns precheck, preparation and/or configuration tasks for the **sql_schema** scope. Current files are an implementation scaffold; executable tasks are expanded incrementally as the MVP is built.
