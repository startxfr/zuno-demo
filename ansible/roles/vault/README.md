# Ansible role: vault

This role owns precheck, preparation and/or configuration tasks for the **vault** scope. Current files are an implementation scaffold; executable tasks are expanded incrementally as the MVP is built.
