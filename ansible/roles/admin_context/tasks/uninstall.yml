---
- name: "admin_context | delete the admin-context GitOps Application (d1: startx HelmChartRepository)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/delete_gitops_app.yml"
  vars:
    gitops_app_name: "zuno-admin-context-d1"

- name: "admin_context | delete the admin-context GitOps Application (d0: PriorityClasses)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/delete_gitops_app.yml"
  vars:
    gitops_app_name: "zuno-admin-context-d0"
