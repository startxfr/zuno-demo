apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhbk-operator
  namespace: zuno-auth
  labels:
    zuno.io/managed-by: zuno-ansible
spec:
  channel: __KEYCLOAK_CHANNEL__
  name: rhbk-operator
  source: __KEYCLOAK_CATALOG_SOURCE__
  sourceNamespace: openshift-marketplace
  installPlanApproval: Automatic
