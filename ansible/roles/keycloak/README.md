# Ansible role: keycloak

This role owns precheck, preparation and/or configuration tasks for the **keycloak** scope. Current files are an implementation scaffold; executable tasks are expanded incrementally as the MVP is built.
