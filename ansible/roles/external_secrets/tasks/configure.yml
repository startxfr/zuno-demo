---
    # Discovered, not hardcoded "vault.zuno-data.svc" - same reasoning as
    # ansible/roles/vault/tasks/configure.yml's own "discover the Vault
    # client Service" task (the Helm release name isn't a repository-wide
    # constant, and the chart creates three Services sharing
    # app.kubernetes.io/name=vault). Re-discovered here rather than reused
    # from that role's fact because relying on a fact set by a different
    # role's task file surviving across the day0_configure.yml role loop is
    # fragile; found and fixed a real bug via live-cluster testing on
    # api.demo222.startx.fr, where the static "vault.zuno-data.svc" hostname
    # this used to hardcode doesn't exist (the actual Service is
    # "zuno-vault"), so vault-backend's ClusterSecretStore never became
    # Ready ("dial tcp: lookup vault.zuno-data.svc ...: no such host").
- name: "external_secrets | discover the Vault client Service"
  kubernetes.core.k8s_info:
    kind: Service
    namespace: zuno-data
    label_selectors:
      - "app.kubernetes.io/name=vault"
  register: _eso_vault_services

- name: "external_secrets | select the client Service (excludes the -internal headless and -ui variants)"
  ansible.builtin.set_fact:
    _eso_vault_service_name: >-
      {{
        (_eso_vault_services.resources
         | rejectattr('metadata.labels.vault-internal', 'defined')
         | map(attribute='metadata.name') | list)[0]
      }}

# Re-discovered rather than passed down from install.yml: apply_gitops_app
# .yml's gitops_app_extra_helm_values *replaces* spec.source.helm.values
# wholesale (ADR-0048), so this second call must re-supply
# subscriptionChannel/subscriptionCatalogSource alongside the newly
# discovered vaultServiceName, or the chart would stop rendering its own
# Subscription. Cheap and idempotent - same PackageManifest read as
# install.yml.
- name: "external_secrets | re-look up the external-secrets-operator PackageManifest"
  kubernetes.core.k8s_info:
    api_version: packages.operators.coreos.com/v1
    kind: PackageManifest
    namespace: openshift-marketplace
    name: external-secrets-operator
  register: _eso_pkg_info

- name: "external_secrets | re-select the catalog/channel"
  ansible.builtin.set_fact:
    _eso_catalog_source: "{{ _eso_pkg_info.resources[0].status.catalogSource }}"
    _eso_channel: >-
      {{
        'stable'
        if (_eso_pkg_info.resources[0].status.channels | selectattr('name', 'equalto', 'stable') | list | length) > 0
        else (_eso_pkg_info.resources[0].status.defaultChannel | default(''))
      }}

# ADR-0312: templates/clustersecretstore.yaml.
- name: "external_secrets | apply the external-secrets GitOps Application (with the Vault-backed ClusterSecretStore)"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/apply_gitops_app.yml"
  vars:
    gitops_component_name: "external-secrets"
    gitops_app_name: "zuno-{{ gitops_component_name }}"
    gitops_app_extra_helm_values:
      subscriptionChannel: "{{ _eso_channel }}"
      subscriptionCatalogSource: "{{ _eso_catalog_source }}"
      vaultServiceName: "{{ _eso_vault_service_name }}"
    gitops_app_wait_retries: 30
    gitops_app_wait_delay: 10

- name: "external_secrets | wait for the ClusterSecretStore to report Valid"
  kubernetes.core.k8s_info:
    api_version: external-secrets.io/v1beta1
    kind: ClusterSecretStore
    name: vault-backend
  register: _eso_store
  until: >-
    (_eso_store.resources | default([]) | length) > 0 and
    (_eso_store.resources[0].status.conditions | default([]) | selectattr('type', 'equalto', 'Ready') | map(attribute='status') | list | first | default('False')) == 'True'
  retries: 30
  delay: 10
