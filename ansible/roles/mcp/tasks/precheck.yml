---
- name: "mcp | verify ArgoCD is installed"
  kubernetes.core.k8s_info:
    api_version: apiextensions.k8s.io/v1
    kind: CustomResourceDefinition
    name: applications.argoproj.io
  register: _mcp_precheck_argocd_crd

- name: "mcp | fail if ArgoCD is not yet installed"
  ansible.builtin.fail:
    msg: "applications.argoproj.io CRD not found. Run `make prepare argocd` first."
  when: (_mcp_precheck_argocd_crd.resources | default([]) | length) == 0

- name: "mcp | precheck summary"
  ansible.builtin.debug:
    msg: "ArgoCD is installed; the MCP Gateway can be applied."
