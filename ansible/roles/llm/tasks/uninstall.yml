---
- name: "llm | delete the llm/ai-gateway/agent-runtime ArgoCD Applications"
  ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/delete_gitops_app.yml"
  vars:
    gitops_app_name: "{{ zuno_id }}-{{ item }}"
  loop:
    - llm
    - ai-gateway
    - agent-runtime
