---
# Day 1 install (`make day1 install`). See day1_check.yml for why
# models/sql_schema/mlops are included alongside llm/rag/mcp/agents.
# `namespaces` runs first so the ResourceQuota/NetworkPolicy overlay (its
# Day 1 half - see ansible/roles/namespaces/README.md) is in place before
# any other Day 1 workload rolls out.
- name: "Day 1: install components"
  hosts: localhost
  gather_facts: false
  environment: "{{ k8s_auth_environment | default({}) }}"
  vars:
    day1_components:
      - namespaces
      - llm
      - models
      - sql_schema
      - rag
      - mcp
      - agents
      - mlops
  tasks:
    - name: Resolve cluster API credentials
      ansible.builtin.include_tasks: "{{ zuno_repo_root }}/ansible/tasks/load_k8s_auth_env.yml"

    - name: Resolve requested Day 1 component name(s)
      ansible.builtin.set_fact:
        selected_components: >-
          {{ day1_components if target_component | default('all') == 'all'
             else [target_component | replace('-', '_')] }}

    - name: Install Day 1 components
      ansible.builtin.include_role:
        name: "{{ item }}"
        tasks_from: "{{ 'install_d1' if item == 'namespaces' else 'install' }}"
      loop: "{{ selected_components }}"
      loop_control:
        label: "{{ item }}"
