all:
  hosts:
    localhost:
      ansible_connection: local
      # Auto-detection can land on a system Python lacking the `kubernetes`
      # pip package these modules need - pin to the interpreter already
      # running ansible-playbook.
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
