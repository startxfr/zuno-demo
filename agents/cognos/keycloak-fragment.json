{
  "_comment": "ADR-0410/ADR-0307 (WP-41) scaffold output - NOT auto-merged. A human merges the relevant pieces into gitops/charts/keycloak/files/realm-zuno.json by hand (see agents/cognos/NEXT_STEPS.md), the same way every prior agent's realm entries were added - see agent_arkos's own client/group/user entries in that file as the worked example.",
  "client": {
    "clientId": "cognos-frontend",
    "name": "Cognos frontend",
    "publicClient": false,
    "secret": "${{vault.cognos_frontend_client_secret}}"
  },
  "group": {
    "name": "agent_cognos",
    "path": "/agent_cognos",
    "clientRoles": {
      "cognos-frontend": [
        "access"
      ]
    }
  },
  "entitlement_only_fixture_user": "cognos-entitlement-only-user-01"
}
