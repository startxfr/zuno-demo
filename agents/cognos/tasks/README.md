# Cognos Tasks

One OKF Markdown bundle (ADR-0038), linked from `../agent.okf.md`'s
`zuno.tasks`: `coming-soon.md` - the placeholder task (ADR-0349 §6, no
runtime exists). Real tasks arrive via the ADR-0307 template workflow
when Cognos is built.
