# Finage Tasks

`coming-soon.md` (ADR-0038 OKF Markdown bundle format), linked from
`../agent.okf.md`'s `zuno.tasks`. Real per-task Markdown documents will be
added here as Finage's v1 tasks are actually implemented.
