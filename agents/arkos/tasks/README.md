# Arkos Tasks

One OKF Markdown bundle (ADR-0038), linked from `../agent.okf.md`'s
`zuno.tasks`: `draft-architecture-testimonial.md` - the only task with a
live Agent Runtime route in v0 (`POST /v1/agents/arkos/chat`, ADR-0342's
`plan_draft_write` graph shape).
