# Identity and Access

Keycloak is the source for application authentication and authorization. Agent groups follow `agent_<name>`. Task permissions may further restrict operations. Google Workspace delegated OAuth preserves end-user permissions for Gmail and Drive integrations.
