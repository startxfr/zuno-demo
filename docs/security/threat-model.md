# Threat Model

Initial threats include prompt injection through RAG documents, unauthorized tool invocation, cross-agent data leakage, SaaS context leakage, credential theft, model-provider misuse, unsafe generated SQL and excessive agent delegation. Detailed mitigations are linked to ADRs and implementation controls.
