# Access-intent matrix

**This document is derived output, not an authorization source.** Per
ADR-0340's own Decision text: "A role matrix is documentation/derived
output, not an independent authorization source: effective authorization
remains the intersection of Keycloak business role, agent/task OKF
declarations, classification/data policy and platform policy." A caller
whose row/column shows access here can still be denied by any of the
other four ADR-0011/ADR-0203 intersection factors (agent declaration,
task rights, classification, or the capability simply not existing yet -
Salesforce, Jira and Workday all have no live backend today). Nothing
reads this file at runtime.

Generated by hand from the authoritative policy files below, current as
of WP-32 (ADR-0340). If this drifts from those files, the files win -
regenerate this page, don't edit policy to match a stale copy here.

Source files:

- [`policies/knowledge/knowledge-policy.yaml`](../../policies/knowledge/knowledge-policy.yaml)
- [`policies/tools/tool-policy.yaml`](../../policies/tools/tool-policy.yaml)
- [`gitops/charts/keycloak/files/realm-zuno.json`](../../gitops/charts/keycloak/files/realm-zuno.json) (business-role group definitions)

## Business roles (ADR-0340 conceptual mapping)

| Population | Keycloak group |
|---|---|
| Technical / consultants | `consultant` |
| Project managers | `cdp` |
| Sales | `sales` |
| Sales administration (ADV) | `adv` |
| Direction | `board` |
| Finance | `finance` |

`architecture`/`build`/`run` are technical skill/data scopes (the
`consultant/confluence-{archi,build,run}-*` RAG-ingestion ACL subgroups),
not business roles - do not confuse them with the six rows above.
Agent entitlement (`agent_<name>` groups, ADR-0040) is a fully orthogonal
dimension: it gates which agent frontend a user may open at all, never
what that user can do once inside one. A user needs both an entitlement
group and a business role for any capability below to actually resolve.

## Knowledge domains (`policies/knowledge/knowledge-policy.yaml`)

| Domain | consultant | cdp | sales | adv | board | finance |
|---|---|---|---|---|---|---|
| `knowledge.tech` | ✓ | ✓ | – | – | ✓ | – |
| `knowledge.sales` | – | ✓ | ✓ | ✓ | ✓ | ✓ |
| `knowledge.sxa-legacy` | – | – | ✓ | ✓ | ✓ | ✓ |
| `knowledge.adv` | – | ✓ | – | ✓ | ✓ | ✓ |
| `knowledge.project` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

`knowledge.project` is broad by design (ADR-0209): membership in the
per-engagement `project_memberships` table is the real gate, a sixth
factor beyond the five this table otherwise reflects - group membership
alone was never meant to be restrictive here.

## Tool capabilities (`policies/tools/tool-policy.yaml`)

| Capability family | consultant | cdp | sales | adv | board | finance |
|---|---|---|---|---|---|---|
| Confluence (`confluence.page.*`) | RW | RW | – | – | RW¹ | – |
| Drive (`drive.document.*`) | RW | R/W² | – | – | RW¹ | – |
| Gmail (`gmail.message.read`) | R | R | – | – | R¹ | – |
| Jira | – | – | – | – | – | – |
| Workday self (`workday.profile.self.*`) | RW | R | – | – | – | – |
| Workday any (`workday.profile.any.read`) | – | R | – | – | – | – |
| Calendar/Meet | – | – | – | – | – | – |

¹ `board`'s Confluence/Drive/Gmail access predates this WP (ADR-0326's
Arkos slice) - unchanged here, just reflected for completeness.
² `list_drive_files`/`drive.document.create`/`.update` currently share
one `allowed_groups` list (no narrower write-specific role exists yet in
this demo's threat model) - `cdp` reads and writes exactly like
`consultant` today; a future WP could split Drive into `self`/`any`-style
scoped capabilities the way Workday already is, if a real need arises.
³ ADR-0219 (2026-08-26) removed the `sxa.*` capability row that used to sit
here: legacy SXA has no deterministic tool path any more, only retrieval over
`knowledge.sxa-legacy` above. That domain's row widened from `[sales, board]`
to `[sales, board, adv, finance]` in the same decision - the union of what it
and the retired `knowledge.sxa` domain granted, so no agent lost reach. This
amends ADR-0340's own table, which marked `adv`/`finance` excluded here.
`consultant` and `cdp` stay "–": technical consultants have no
commercial-data access by design, and no need was stated for `cdp`. The
`sales.*` namespace remains reserved for a live Salesforce server (WP-33).

Workday has no live backend yet (`platform/bindings/tools/tool-bindings.yaml`'s
three `workday.profile.*` bindings are `auth_mode: provider-delegated`
with no provider bound - every call fails closed with `501` regardless of
what this table or the policy files say, per ADR-0208's third auth mode).
The scoped capability pattern (`self.read`/`self.update`/`any.read`,
`any.update` deliberately never introduced) is what this WP actually
proves: a technical user updates only their own profile, `cdp` reads any
authorized profile but can never write one, matching ADR-0340's
acceptance criteria exactly.
