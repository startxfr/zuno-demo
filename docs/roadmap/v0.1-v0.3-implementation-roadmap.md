# v0.1 – v0.3 implementation roadmap

**Purpose:** drive every open ADR in the v0.1, v0.2 and v0.3 streams to
`Implemented`. The work is decomposed into work packages (WPs), each with a
self-contained brief under [work-packages/](work-packages/) written so a
lower-capability model can execute it as a standalone task.

[docs/adr/README.md](../adr/README.md) is the **sole authority for ADR
status**. This roadmap tracks WP state only — never copy ADR status strings
here (`platform/docs/check_docs.py` validates the index/body pair; a third
copy would drift unvalidated).

## Scope: the 40 open ADRs

- **v0.1 (17):** 0101–0110, 0112 (stubs in `adr-decisions-v0.1.md`), 0350
  (ex-0113, re-streamed to v0.3 on 2026-08-15), 0114,
  0116, 0117, 0322 and 0330 (retargeted from v0/v0.1 to v0.1 on
  2026-08-16). (0115 retargeted to v0.7 on 2026-08-24 — GitHub Actions
  release-pipeline automation, see `versions.md`. 0111 retargeted to v0.7
  on 2026-08-26 — grouped with ADR-0115 under the same WP-04
  billing-lock blocker, see `versions.md`. 0105/WP-22 retargeted to v0.7
  on 2026-08-26 — an unrelated roadmap-reprioritization group, see
  `versions.md`.)
  Six later additions, outside the original 40-ADR count: ADR-0119/ADR-0120
  (2026-08-19, WP-057/WP-058), the first GitHub/GitLab MCP integration,
  ADR-0121 (2026-08-20, WP-059), wiring Tekos/Arkos to it, ADR-0215
  (2026-08-20, WP-060), carrying conversation history into agent prompts
  with budgeted compaction, and ADR-0057/ADR-0058 (2026-08-21,
  WP-062/WP-063), Day 2 agent availability-test and stresstest operations.
- **v0.2 (7):** 0202, 0203, 0204, 0205, 0206, 0208, 0209. (0201 retargeted
  to v0.5 on 2026-08-24 — making the MaaS governance plane live and used
  by agents, see `versions.md`. 0206/WP-23 retargeted to v0.7 on
  2026-08-26 — grouped with 0105/WP-22 in the same unrelated
  roadmap-reprioritization group, see `versions.md`.)
- **v0.3 (14):** 0301, 0302, 0303–0309 (stubs in `adr-decisions-v0.3.md`),
  0326, 0327, 0340, 0342, 0353 (stub in `adr-decisions-v0.3.md`). (0201,
  0511, 0512 passed through this scope briefly on 2026-08-24 before
  moving on to the new v0.5 MaaS milestone the same day; see
  `versions.md` and `docs/adr/README.md`'s Retargeting notes.)

## Execution model

- **1 WP = 1 brief = 1 reviewable change-set** (large WPs split into
  lettered parts, each independently committable).
- Every brief separates **repo changes** (model-executable, verifiable from
  the repository) from **operator/human steps** (cluster, credentials, GPU,
  external services). Repo work merging moves the ADR to
  `Partially implemented` with the residual operator actions enumerated in
  the ADR body (the ADR-0115 gap-list pattern); the listed operator
  confirmations then move it to `Implemented`. ADRs with no operator
  dependency go straight to `Implemented` on merge.
- Stub ADRs are **promoted to full ADR files first** (Step 0 of their
  brief), following the repository's promotion convention: the roadmap
  container keeps the `### ADR-0NNN` heading (and any explicit anchors),
  its body becomes a pointer line, the index row flips to a direct link,
  numbers are retained.
- **WP state machine:** `Not started → Repo work in review → Repo work
  merged → Operator pending → Done`, plus three terminal states for work that
  stops early: `Abandoned` (superseded by another decision), `Cancelled`
  (deprioritized), `Closed — deferred` (blocked outside this repo). A WP that
  merged code but was then superseded is `Abandoned`, and its brief must say
  what landed - see WP-065/WP-066. `Done` requires the brief's
  Status-updates section to have been executed (ADR body + index + tracker +
  brief + MEMORY.md) and `python3 platform/docs/check_docs.py` to pass.
- **Immutability boundary:** only ADR `**Status:**` lines, dated
  gap/progress lists, and stub-body promotion pointers are editable.
  A change of direction requires a superseding ADR, never an edit.
- **Standing rules for every WP:** never touch the uncommitted ADR-0344
  change set if still present; new Dockerfiles enter
  `.github/workflows/build-publish.yml`'s matrix; new Makefile components
  need matching `ansible/roles/<name>[_build]`; new charts pass
  `platform/security/check_workload_hardening.py`; WP-04 stage 3 (which
  would have made `check_no_latest_tags.py` blocking) is deferred
  (2026-08-22, ADR-0115 closed) — charts keep `tag: latest` until a future
  ADR reactivates that stream; Makefile verbs are the source of
  truth (`d0: check install uninstall reconcile all`;
  `d1: check install build uninstall reconcile all`).

## Tracker

Update the **State** column as WPs progress; everything else is fixed at
authoring time. States: `Not started | Repo work in review | Repo work
merged | Operator pending | Done | Abandoned | Cancelled | Closed — deferred`.
`platform/docs/check_docs.py` enforces this vocabulary AND that each row's
State matches its brief's own `- **State:**` line.

### Phase 0 — baseline

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-00 | (executed inline, 2026-08-13) | — | — | Done | none — `check_docs.py` green |

### Phase 1 — v0.1: platform trust and tool foundation

Order rationale: the binding registry (WP-01) is the prerequisite for
Confluence (WP-02), auth modes (WP-26), scoped capabilities (WP-32) and all
agent rollouts. The supply-chain release (WP-04) is closed — deferred
(2026-08-22, ADR-0115); its one real release already unblocked OKF signing
(WP-05)'s cosign tooling, and no further supply-chain work is planned until
a future ADR reactivates the stream. WP-06/WP-07 (RAG platform) and
WP-08/WP-09 (runtime capabilities) are
independent tracks; evaluation and ops packages (WP-10–WP-13) close the
phase.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-01 | [wp-01](work-packages/wp-01-logical-tool-bindings.md) | 0116 | WP-00 | Done | none |
| WP-02 | [wp-02](work-packages/wp-02-confluence-mcp-server.md) | 0117 | WP-01 | Done (2026-08-18 — Atlassian grant landed, live e2e run recorded in the brief) | none |
| WP-03 | [wp-03](work-packages/wp-03-maas-adapter-prototype.md) | 0114 | WP-00 | Done (2026-08-18 — decision-risk path taken: ADR-0114 superseded by ADR-0118, cutover decision moved to WP-27) | none (live MaaS verification is WP-27's) |
| WP-04 | [wp-04](work-packages/wp-04-supply-chain-completion.md) | 0115 | WP-00 | Closed — deferred (2026-08-22 — pipeline disabled, stage 3 not pursued; resumes under a future ADR) | none (deferred) |
| WP-05 | [wp-05](work-packages/wp-05-okf-bundle-signing.md) | 0106 | WP-04 st.1 | Done (operator follow-up closed by WP-069, 2026-08-22/25) | first real signed bundle |
| WP-06 | [wp-06](work-packages/wp-06-ogx-migration.md) | 0322 | WP-00 | Done (2026-08-21 — corpus proof + live provider-parity run both complete; found+fixed a real ACL-array bug in the OGX provider along the way) | none |
| WP-07 | [wp-07](work-packages/wp-07-rag-ingestion-catalog.md) | 0330 | WP-00 | Done (2026-08-18 — real space keys mapped, KFP recurring-run activated live, two payload bugs found+fixed) | none |
| WP-08 | [wp-08](work-packages/wp-08-resumable-workflows.md) | 0103 (P) | WP-00 | Done | none (optional drill) |
| WP-09 | [wp-09](work-packages/wp-09-semantic-caching.md) | 0104 (P) | WP-00 | Done | none |
| WP-10 | [wp-10](work-packages/wp-10-model-quality-gates.md) | 0107 (Implemented), 0108 (Implemented) | WP-00 | Done (2026-08-22 — ADR-0107 discharged 2026-08-21; ADR-0108's `LMEvalJob.status.state` operator bug closed via a live-verified `outputs.pvcManaged` PVC workaround in `evaluations/benchmark.py`, not an upstream fix) | none |
| WP-11 | [wp-11](work-packages/wp-11-secnumcloud-hardening.md) | 0111 (P) | WP-00 | Done (matrix tracks further gap rows owned by other WPs) | none (deferred, see ADR-0111) |
| WP-12 | [wp-12](work-packages/wp-12-ha-availability.md) | 0101, 0102 (P) | WP-00 | Done (2026-08-18 — drill + short-window measurement recorded in `docs/platform/slo.md`) | none (30-day SLO series keeps accumulating, complete ~2026-09-17) |
| WP-13 | [wp-13](work-packages/wp-13-backup-recovery.md) | 0112 (P) | WP-00 | Done (2026-08-18 — both restore drills executed, recorded in `docs/platform/backup-recovery.md`) | none |

(P) = brief starts with a stub-ADR promotion.

### Phase 2 — v0.2: knowledge governance (+ v0.1 carve-outs)

Order rationale: the strict spine is WP-20 (domains + policy) → WP-21
(multi-domain core) → WP-22 (adapters + cadences). WP-23/24/25 build on the
spine; WP-26 needs only WP-01; WP-27 follows WP-03; WP-28 lands last (it
touches the BFF contract and its final acceptance is a Phase 3 carve-out).

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-20 | [wp-20](work-packages/wp-20-knowledge-domains-policy.md) | 0202, 0203 | WP-00 | Done | none |
| WP-21 | [wp-21](work-packages/wp-21-multi-domain-rag-core.md) | 0204 pt1 | WP-20 | Done | none |
| WP-22 | [wp-22](work-packages/wp-22-ingestion-adapters-cadences.md) | 0204 pt2, 0105 (P); Salesforce/Aramis clauses superseded by 0218 (2026-08-23, amended 2026-08-26) | WP-21, WP-07 | Done (2026-08-26 — tech/legacy live cadence runs completed) | none — SXA snapshot is WP-23's action, not this WP's; Aramis adapter removed from the repo and Salesforce cadence deferred to v0.7 (ADR-0218) |
| WP-23 | [wp-23](work-packages/wp-23-sales-sxa-separation.md) | 0206 (deterministic-MCP half withdrawn by 0219) | WP-20, WP-21 | Done (2026-08-26) | none — the "real SXA snapshot load" action is closed by removal: ADR-0219 retired the deterministic path entirely, see Phase 14; the deferred field-level classification review is closed by decision on 2026-08-30 (C3 → C2, see WP-23's Status updates) |
| WP-24 | [wp-24](work-packages/wp-24-freshness-trust-scoring.md) | 0205, 0109 (P) | WP-20, WP-21, WP-01 | Done | none (optional live check) |
| WP-25 | [wp-25](work-packages/wp-25-document-acl-sync.md) | 0110 (Implemented) | WP-21, WP-02 | Done | - |
| WP-26 | [wp-26](work-packages/wp-26-binding-auth-modes.md) | 0208 | WP-01 | Done | none (optional revocation check) |
| WP-27 | [wp-27](work-packages/wp-27-maas-governance.md) | 0201 | WP-03 | Done (2026-08-25 - governance plane live end-to-end; the 2026-08-24 note below is kept for the retraction it records — the wasm-shim defect this row previously blamed is retracted: WP-071 root-caused and fixed a locally-fixable Authorino/Envoy TLS trust mismatch instead, live-verified on `maas-default-gateway` — `401`, not `500`, ext_authz transport works end to end. See ADR-0201's 2026-08-24 part 5 note. The separately-documented RHOAI payload-processing `EnvoyFilter` subfilter-anchor gap remains open — MaaS auth enforcement is transport-capable now, not yet proven attached to live model requests) | none from this repo for the TLS/transport path (resolved by WP-071); the payload-processing anchor gap still needs RHOAI-side investigation — see ADR-0201 parts 1-2 |
| WP-071 | [wp-071](work-packages/wp-071-authorino-service-ca-trust-alignment.md) | 0201, 0511 | WP-27, WP-54 | Done (2026-08-24 — Authorino/Envoy TLS trust mismatch root-caused and fixed live; `maas-default-gateway` returns 401, not 500) | none |
| WP-28 | [wp-28](work-packages/wp-28-project-memory.md) | 0209 | WP-20, WP-21 | Done (2026-08-23 — tracker correction: WP-31's own 2026-08-15 merge already flipped ADR-0209 to `Implemented` and passed its cross-agent acceptance test (Tekos writes a `demo-001` fact, Arkos retrieves it); this row had not been updated to reflect that closure) | none |

### Phase 3 — v0.3: multi-agent rollout, MLOps, operator

Order rationale: WP-30 (multi-shape runtime) precedes any second agent.
Agents follow ADR-0326's sequence Arkos → Comage → Advantage → Finage, with
WP-32 (roles) before Comage. The MLOps track (WP-34) starts once Comage's
bundle exists and runs in parallel. WP-37 (CRD contract) needs two real
agents; WP-38 (operator) closes ADR-0350. WP-39–WP-42 close the roadmap.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-30 | [wp-30](work-packages/wp-30-multi-shape-runtime.md) | 0342 | WP-00 (WP-28 rec.) | Done | none |
| WP-31 | [wp-31](work-packages/wp-31-arkos-slice.md) | 0326 (capstone); closes 0209+0342 | WP-30, WP-28, WP-26, Ph.2 | Done (2026-08-30 — live gate, retroactive) | none - scenario 9 (structure-demo) timeout is a known, non-blocking finding |
| WP-32 | [wp-32](work-packages/wp-32-cdp-scoped-capabilities.md) | 0340 | WP-01, WP-20 | Done | none |
| WP-33 | [wp-33](work-packages/wp-33-comage-slice.md) | 0326 (capstone) | WP-31, WP-22 (adapters merged, non-blocking), WP-23, WP-24, WP-32 | Done (2026-08-30 — live gate, retroactive) | live Salesforce read/write carved out to WP-101 (v0.7) |
| WP-34 | [wp-34](work-packages/wp-34-lora-mlops.md) | 0302, 0301 | WP-33a, WP-10 | Operator pending | GPU pipeline run; promotion PR; adapter download wiring |
| WP-35 | [wp-35](work-packages/wp-35-advantage-slice.md) | 0326 (capstone); closes 0532 | WP-33, WP-22 (adapter merged, non-blocking) | Done (2026-08-30 — closed placeholder, ADR-0532) | none - stays `placeholder` by decision |
| WP-36 | [wp-36](work-packages/wp-36-finage-slice.md) | 0326 (capstone, closes it) | WP-35 | Done (2026-08-30 — closed placeholder, D10) | none - stays `placeholder` by decision |
| WP-37 | [wp-37](work-packages/wp-37-aiagent-crd-contract.md) | 0327 | WP-31 (+WP-33) | Done | none |
| WP-38 | [wp-38](work-packages/wp-38-aiagent-operator.md) | 0308; closes 0350 (ex-0113) | WP-37 | Done | none (fleet-wide ADR-0053 gate has pre-existing, unrelated gaps - see WP-31/33/35/36) |
| WP-39 | [wp-39](work-packages/wp-39-dynamic-lora.md) | 0303 (P) | WP-34 | Closed — deferred (2026-08-30 — ADR-0303 superseded by ADR-0526) | none (deferred) |
| WP-40 | [wp-40](work-packages/wp-40-benchmarking-routing.md) | 0305, 0304 (P) | WP-10, WP-34 | Done | one live benchmark+report loop |
| WP-41 | [wp-41](work-packages/wp-41-onboarding-catalog.md) | 0307, 0410 (ex-0306) - retargeted to v0.9 (2026-08-30, via v0.7 same day), Proposed | WP-38, WP-36 | Cancelled (2026-08-23) | none (cancelled) |
| WP-42 | [wp-42](work-packages/wp-42-autonomous-optimization.md) | 0309 | WP-40, WP-09 | Done | none |

## Phase 5: git-forge integration (added 2026-08-19/20, outside the original 40-ADR scope)

WP-057 builds the scaffolding/conformance tooling ADR-0119 decides; WP-058
is its first real consumer, the GitHub/GitLab MCP server ADR-0120 decides;
WP-059 wires Tekos and Arkos to it with the public/private access split
ADR-0121 decides.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-057 | [wp-057](work-packages/wp-057-mcp-scaffolding-tooling.md) | 0119 | WP-01 | Done | none |
| WP-059 | [wp-059](work-packages/wp-059-git-forge-tekos-arkos-wiring.md) | 0121 | WP-058 | Done | none |
| WP-058 | [wp-058](work-packages/wp-058-git-forge-mcp-server.md) | 0120 | WP-057 | Done | none (read + delete-refusal live-verified both providers 2026-08-23; write/create/fork remain test-only by choice) |

## Phase 6: conversation history and compaction (added 2026-08-20, outside the original 40-ADR scope)

WP-060 makes every agent multi-turn: history reconstructed server-side from
ADR-0103's own checkpoints, injected into each model call under a token
budget, with older turns auto-summarized when that budget is exceeded -
ADR-0215 decides it.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-060 | [wp-060](work-packages/wp-060-conversation-history-compaction.md) | 0215 | WP-08, WP-30, WP-31, WP-33 | Done | none - live two-turn verification per agent confirmed on the real cluster (2026-08-21) |

## Phase 7: Day 2 operations (added 2026-08-21, outside the original 40-ADR scope)

WP-062 builds the Day 2 chassis ADR-0057 decides: the `make d2
test|stresstest` Makefile/Ansible dispatch, dynamic OKF-bundle-driven
agent/service discovery, the shared text/JSON/CSV report engine, and the
availability-only `d2 test` command (also collapsing `check.yml`'s six
hand-copied per-agent `/healthz` blocks into one loop). WP-063 builds the
stresstest content ADR-0058 decides: aggregating every agent's existing
contract/scenario/security/gate/stress-test content through that chassis,
generalizing the acceptance-gate Job from Tekos-only, and adding the new
bulk-interaction load mode.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-062 | [wp-062](work-packages/wp-062-day2-test-chassis.md) | 0057 | WP-31, WP-43 | Done | none - live run 2026-08-21: 14/14 passed |
| WP-063 | [wp-063](work-packages/wp-063-day2-stresstest-content-aggregation.md) | 0058 | WP-062 | Done | none - live run 2026-08-21 (BULK=25): mechanism proven, 166/178 content passed (12 real app-layer failures logged, see WP-063) |

## Phase 8: build/deploy trigger formalization (added 2026-08-21, outside the original 40-ADR scope)

WP-064 formalizes ADR-0059: the already-shipped `image.openshift.io/
triggers` mechanism (commit `649243c`) that auto-redeploys a component's
running pods the moment its in-cluster `Build` completes, with matching
ArgoCD `ignoreDifferences` and an `aiagent-operator` reconcile-loop guard
so neither fights the trigger's patch. Retroactive documentation of
working code, not new implementation - see WP-064's own brief for the
live regression it also found and fixed (this session's own WP-04
release pin had briefly broken it for 16 components).

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-064 | [wp-064](work-packages/wp-064-auto-redeploy-trigger-formalization.md) | 0059 | — | Done | none |

## Phase 9: real SXA content (added 2026-08-21, outside the original 40-ADR scope)

**Abandoned 2026-08-26 — superseded by Phase 14 (ADR-0219/WP-084).** WP-065
delivered ADR-0216's Part A: a real SXA mysqldump imported natively into a
new MariaDB database on the shared instance, served through the `sales-db`
MCP server for exact lookups and through the RAG pipeline for semantic
recall. Part B's live steps never ran. ADR-0219 then withdrew the decision:
SXA is a closed pre-2021 record with no live system behind it, so the
MariaDB store and the MCP tool surface are deleted rather than completed.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-065 | [wp-065](work-packages/wp-065-sxa-mariadb-import-rag.md) | 0216 (Superseded by 0219) | WP-23 | Abandoned | none — superseded by WP-084 |

## Phase 10: role-based conversation sharing (added 2026-08-21, outside the original 40-ADR scope)

WP-066 delivers ADR-0213: an owner can grant a colleague reader/actor/
cloner rights on a conversation, a single-active-writer lease prevents two
collaborators racing the same LangGraph checkpoint thread, and the
frontend gains a share dialog plus the kebab's two remaining actions
(Share, Clone) that WP-061 explicitly deferred to this WP. The colleague-
search feature depends on a new Keycloak Admin API trust boundary
(`zuno-admin-api` confidential client) that the ADR itself says needs
explicit reviewer sign-off — the code ships complete and fails closed
(503) until that client is provisioned as a separate operator step; see
WP-066's own Scoping decision note.

**Abandoned 2026-08-27 — superseded by Phase 21.** ADR-0527 moves sharing from
the conversation to the project, so `conversation_memberships`, the
reader/actor/cloner vocabulary and `ShareDialog.tsx` are removed rather than
migrated — safe precisely because the operator step below never ran, so no
sharing row ever existed in service. The write lease and the Keycloak Admin API
trust boundary both survive into WP-088.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-066 | [wp-066](work-packages/wp-066-role-based-conversation-sharing.md) | 0213 (Superseded by 0527) | ADR-0212, WP-061 | Abandoned 2026-08-27 — superseded by Phase 21 (WP-088/WP-089) | none — the `zuno-admin-api` provisioning it needed carries over to WP-088, which reuses `GET /api/colleagues` |

## Phase 11: weekly SXA RAG corpus (added 2026-08-21, outside the original 40-ADR scope)

**Abandoned 2026-08-26 — superseded by Phase 14 (ADR-0219/WP-084).** WP-067
delivered ADR-0217: a second SXA source parsed directly in Python (no
MariaDB, no MCP tools) into a new `knowledge.sxa` RAG domain, opened to
Comage/Advantage/Finage. It turned out to read the same bucket and the same
bytes as `knowledge.sxa-legacy`, so ADR-0219 kept one domain. Its pure-Python
parser survives as `load-sxa-dump`'s implementation, and the access it granted
was preserved by widening `knowledge.sxa-legacy`'s `allowed_groups`.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-067 | [wp-067](work-packages/wp-067-sxa-weekly-rag-corpus.md) | 0217 (Superseded by 0219) | — | Abandoned | none — superseded by WP-084 |

## Phase 12: in-cluster supply-chain signing (added 2026-08-22, outside the original 40-ADR scope)

ADR-0420 replaces ADR-0106/ADR-0115's GitHub-OIDC/Fulcio/Rekor keyless
signing (external SaaS: GitHub Actions, Sigstore's public Fulcio/Rekor,
`quay.io`) with a fully in-cluster mechanism built on Vault Transit — a
stated demo goal ("running all inside the cluster"). WP-068 stands up the
signing backend alone, zero consumers cut over. WP-069/070 cut OKF bundle
signing and image signing over to it - both live-verified 2026-08-22, each
keeping its existing enforcement flag off pending an explicit operator
go-ahead.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-068 | [wp-068](work-packages/wp-068-vault-transit-signing-backend.md) | 0420 | — | Done | none — live-verified 2026-08-22: sign/verify/tamper/negative-auth/non-exportability/offline-verify/audit-trail all confirmed on api.demo222.startx.fr |
| WP-069 | [wp-069](work-packages/wp-069-in-cluster-okf-bundle-signing.md) | 0106 (mechanism superseded by 0420) | WP-068 | Done | none - `requireSignedBundles: true` flipped and live on the running Deployment 2026-08-22, all 8 agents verified with zero errors |
| WP-070 | [wp-070](work-packages/wp-070-in-cluster-image-signing.md) | 0115 (mechanism superseded by 0420) | WP-068 | Done | none - fix aiagent-operator's missing Makefile/build-component wiring only if a future WP needs it (pre-existing gap, deliberately out of scope here) |

## Phase 13: Ansible Automation Platform (added 2026-08-24, outside the original 40-ADR scope)

ADR-0354 (amended 2026-08-24: Day 0 → Day 1 placement post-ADR-0060,
retargeted v0.3 → v0.2) installs AAP as a new Day 1 component (`aap`) plus
a companion `aap-config` component registering this repository as an AAP
Project with a single `day0_check` Job Template. WP-072 covers `aap`
itself and ends with a live CRD inventory that decides `aap-config`'s
implementation mechanism; WP-073 implements `aap-config` from that
inventory. ADR-0355 (v0.3, new companion ADR) then exposes both a
read-only platform-health audit and a Job-Template-launching cluster audit
to Tekos and Arkos through a new `mcp-aap` server (WP-074) - the first
agent-reachable capability in this repository that runs cluster
automation rather than only reading state.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-072 | [wp-072](work-packages/wp-072-aap-day1-component.md) | 0354 | postgresql, vault, external-secrets, keycloak, openshift-oauth | Done (live-verified 2026-08-25) - Gateway/Controller/Hub/EDA Running on demo222, RHN subscription auto-attach confirmed, 3 platform bugs fixed en route | none |
| WP-073 | [wp-073](work-packages/wp-073-aap-config-project-and-day0-check-job-template.md) | 0354 | WP-072 | Done (live-verified 2026-08-25) - Path A + 4 approved additions live-confirmed (org `zuno`, Project/Job Template/credential CRs reconciled, Keycloak authenticator wired); 5 real bugs fixed en route; vault `pods/exec` gap accepted as a documented limitation | interactive browser Keycloak SSO login still unverified (API-level only) |
| WP-074 | [wp-074](work-packages/wp-074-mcp-aap-server.md) | 0355 | WP-072, WP-073 | Done (live-verified 2026-08-27 — both tools exercised against real AAP; `cluster_audit` launched job 76 as `zuno-mcp`) | none — two live defects found and fixed en route (Platform Auditor is a role, not a user flag; the gate had no allow-path check) |
| WP-075 | [wp-075](work-packages/wp-075-aap-hub-s3-storage.md) | 0354 | WP-072 | Done (live-verified 2026-08-25 — Hub content on S3; podAffinity is the real fix, S3 does not remove the local PVC) | none |
| WP-077 | [wp-077](work-packages/wp-077-aap-postgres-uninstall-cleanup.md) | 0354 | WP-072, WP-075 | Done (live-verified 2026-08-25 — a full uninstall/install cycle ran clean with zero manual intervention) | none |

## Phase 14: SXA as a RAG-only historical corpus (added 2026-08-26, outside the original 40-ADR scope)

WP-084 delivers ADR-0219, which supersedes ADR-0216 and ADR-0217 in full and
closes out Phases 9 and 11. SXA is the company's commercial record from
before 2021: there is no live system behind it, so the deterministic MCP path
ADR-0216 built has nothing to be authoritative about, and the two RAG domains
were already reading the same bytes from the same bucket. One domain
(`knowledge.sxa-legacy`) survives, widened to `[sales, board, adv, finance]`
so no agent loses reach; `load-sxa-dump` keeps its name and drops MariaDB;
the `sales-db` MCP server, the `sql-schema` Day-2 component, `data/sxa/` and
the MariaDB `sxa` database are deleted. ADR-0016 becomes superseded in full.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-084 | [wp-084](work-packages/wp-084-retire-the-sxa-mcp-path-and-second-rag-domain.md) | 0219 (Implemented); supersedes 0216, 0217, 0016 | WP-065, WP-067 | Done (2026-08-27 — teardown, re-ingestion and verification all complete; `rag-sxa-legacy` independently confirmed at 319,713 chunks) | none |

## Phase 15: RAG ingestion throughput (added 2026-08-27, outside the original 40-ADR scope)

Two mechanical performance decisions against `components/rag-ingestion/`, taken once ADR-0518's
full re-ingestion made the existing stage timings untenable. WP-57 parallelizes and
short-circuits the fetch stages (thread pools, conditional GET with stored ETag, a sha256
dump-checksum skip); WP-58 then parallelizes the `detect-changes` per-document S3 GETs, the
bottleneck WP-57's fix exposed. Note the numbering: these are **not** WP-057/WP-058, which are
different work packages (`wp-057-mcp-scaffolding-tooling`, `wp-058-git-forge-mcp-server`).

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-57 | [wp-57](work-packages/wp-57-accelerate-rag-ingestion-fetch-stages.md) | 0519 | ADR-0518's re-ingestion | Done (2026-08-25 — live-verified: the SXA fetch stage went from 127min+ and incomplete to 35m27s, CPU 4.3% → 89%+) | none |
| WP-58 | [wp-58](work-packages/wp-58-parallelize-detect-changes-read.md) | 0520 | WP-57 | Done (2026-08-25 — live-verified: `detect-changes` 3h48m17s → 13m25s, ~17x; full pipeline SUCCEEDED in 16m51s) | none — `readConcurrency` retuned 16 → 64 by WP-084 since |

## Phase 16: route local model traffic through MaaS (added 2026-08-27, outside the original 40-ADR scope)

WP-076 generalizes WP-27's live MaaS proof: ai-gateway's own local-model calls now go through the
MaaS governance plane rather than around it, with a drill-proven direct fallback.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-076 | [wp-076](work-packages/wp-076-maas-local-traffic.md) | 0521 | WP-27 | Done (2026-08-26 — all five steps live-verified, both local models routed through MaaS, fallback drill-proven) | none |

## Phase 17: RHOAI side-by-side monitoring (added 2026-08-27, outside the original 40-ADR scope)

Three increments delivering ADR-0522: populate `DSCInitialization.spec.monitoring` so RHOAI
provisions its own metrics, traces and Perses stack in the until-then-empty
`redhat-ods-monitoring`, running fully side-by-side with the hand-deployed `zuno-monitoring` and
touching no application code. Phase 1 deliberately ends with zero cross-stack data flow;
unifying the two stacks is a separate, not-yet-started follow-up (WP-080's "Phase 2").

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-078 | [wp-078](work-packages/wp-078-rhoai-monitoring-metrics.md) | 0522 | ADR-0331 | Done (live-verified 2026-08-26 — MonitoringStack, ThanosQuerier and Alertmanager Running) | none |
| WP-079 | [wp-079](work-packages/wp-079-rhoai-monitoring-traces.md) | 0522 | WP-078 | Done (live-verified 2026-08-26 — full TempoStack Running, 3 live-found bugs fixed same day) | none |
| WP-080 | [wp-080](work-packages/wp-080-rhoai-monitoring-perses-route-dashboard.md) | 0522 | WP-078, WP-079 | Done (live-verified 2026-08-26 — Perses Route 302 → 200; closed ADR-0522's phase-1 scope) | none — RHOAI Dashboard's "Observe" tab not click-through-verified (light follow-up, non-blocking) |
| WP-081 | [wp-081](work-packages/wp-081-mesh-trace-fanout-to-rhoai-collector.md) | 0523 | WP-079, WP-080 | Done (live-verified 2026-08-26 — mesh traces fanned out to RHOAI's collector) | none |
| WP-082 | [wp-082](work-packages/wp-082-model-workload-otel-auto-instrumentation.md) | 0523 | WP-079, WP-080 | Done (live-verified 2026-08-26 on embeddings + qwen; gpt-oss-20b blocked on a separate defect recorded in the brief) | gpt-oss-20b instrumentation only |

## Phase 18: GPU capacity and MIG layout (added 2026-08-27, outside the original 40-ADR scope)

Operator-tier capacity work amending ADR-0351 in place rather than opening a new decision: a
second permanent MIG node, right-sized GPU requests, and the retirement of the IPI GPU node.
WP-086 then spreads the predictors across both MIG nodes and clears two platform-hygiene items.
WP-092 (2026-08-29) later gives WP-086's generic anti-affinity a targeted second term between
`qwen35-9b` and its `qwen35-9b-wesh` fine-tune (WP-087), and investigates-and-rejects a
`2x mig-2g.48gb` repartition of both permanent MIG nodes — recorded in ADR-0414's amendment
rather than the two already-`Done` WPs above.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-083 | [wp-083](work-packages/wp-083-gpu-rightsizing-and-second-mig-node.md) | 0351 (amended in place 2026-08-26) | WP-082, WP-076 | Done (live-verified 2026-08-26 — two permanent MIG nodes live, IPI GPU node decommissioned) | none |
| WP-086 | [wp-086](work-packages/wp-086-spread-models-and-platform-hygiene.md) | none new; relates to 0351 | WP-083 | Done (live-verified 2026-08-26 — machine replaced, predictors spread, all 11 checks passed) | none |
| WP-092 | [wp-092](work-packages/wp-092-qwen35-wesh-targeted-anti-affinity.md) | 0414 (amended 2026-08-29) | WP-086, WP-087 | Done (live-verified 2026-08-29 — placement separated, decisive check run, see the brief's live findings) | none |

## Phase 19: OpenShift Lightspeed as a Zuno consumer (added 2026-08-27, outside the original 40-ADR scope)

WP-085 wires OpenShift Lightspeed to Zuno's own inference, knowledge and identity planes,
reusing ADR-0521's MaaS transport and mirroring the `aap`/`aap-config` two-component split.
**Repo-only so far** — no cluster change has been made, so ADR-0524 correctly remains `Proposed`.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-085 | [wp-085](work-packages/wp-085-openshift-lightspeed-integration.md) | 0524 | WP-076, WP-072/WP-073 | Done (live-verified 2026-08-27 — Lightspeed answers as `consultant-01`/`sale-01`, loads exactly the 2 capped `zuno-mcp` tools, and a real `confluence.page.search` call succeeded under the end user's own identity) | none for this WP — returning inference to MaaS is 2 edits, blocked on a MaaS subject-matching defect (the `qwen36-27b-instruct-lightspeed` subscription is Active, yet the control plane still answers `403 no matching subscription`) |

## Phase 20: French urban-register model variant (added 2026-08-27, outside the original 40-ADR scope)

WP-087 replaces WP-34's `comage-lora` objective, which never ran and had no data behind it
(Comage's two knowledge domains hold zero rows), with a style adaptation whose corpus exists:
a LoRA fine-tune of `Qwen/Qwen3.5-9B` on a staged French urban-register corpus, merged into a
standalone checkpoint and served as `qwen3.5-9b-wesh` beside its unmodified base on a different
MIG node. Comage routes to it first on all four of its tasks, Tekos second on all four of its
own. ADR-0526 supersedes ADR-0301 (decisions 1, 5) and ADR-0302 (decisions 2, 4) in part; every
other decision point of both records stays in effect. Fits the remaining MIG slices exactly,
which saturates the `zuno-ai-run` GPU quota and gives up ADR-0351 decision 5's single-node
survivability — both consequences are recorded in ADR-0526 rather than discovered later.
Repo work merged 2026-08-27. Along the way it closed the two independent reasons WP-34's pipeline had never executed — nothing compiled or uploaded a `PipelineVersion`, and the compiled DAG name (`mlops-<agent>`) referenced a `Pipeline` CR that was never rendered — plus four defects neither the ADR nor the brief listed, each of which would have failed a first run on its own.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-087 | [wp-087](work-packages/wp-087-french-urban-register-model.md) | 0526 | WP-34, WP-076, WP-083/WP-086 | Done (2026-08-29) | Three-half PASS + push-registry (model_version_id 6). Tool calling was broken by the fine-tune and is now gated by a third half; hallucinated-tool ceiling met by neither the variant nor its base; fallback paths untested |

## Phase 21: the project as sharing and context boundary (added 2026-08-27, outside the original 40-ADR scope)

Three unrelated notions of "project" existed and none was an object a user could
create: ADR-0209's mandatory `project_id` metadata key guarded by a binary
`project_memberships` table nobody could populate through an API, ADR-0213's
conversation-level sharing (merged but never provisioned — `GET /api/colleagues`
has always answered 503), and ADR-0512's Salesforce opportunity id doubling as
the project's identity. ADR-0527 unifies them into one first-class object: a
`projects` table that mints every `project_id`, four roles (`read < clone <
write < admin`) granted to subjects **or** business-role groups, no owner column
but a last-admin guard, a 54 000-character engagement context injected as
budgeted background rather than instructions, cascade-archive deletion, and
`project_memberships` demoted to a projection pushed inside the grant-mutation
transaction. ADR-0528 then separates the Salesforce link from the project's
identity — an optional attribute verified at project save, making a project
either *customer* or *free* — and re-keys quota and telemetry onto the Zuno
`project_id`, adding the `zuno.project_id` span attribute that existed nowhere.
ADR-0527 supersedes ADR-0213 in full; ADR-0528 supersedes ADR-0512's clause 3
and its quota/observability keying, leaving clauses 1, 2 and the rest of 4 in
effect.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-088 | [wp-088](work-packages/wp-088-project-entity-and-rbac-backend.md) | 0527 | ADR-0212, ADR-0515 (supersedes WP-066) | Done (2026-08-28) | none — all four components rebuilt and the backend live-verified end to end (create/list/read/archive, projection revoked at archive time) |
| WP-089 | [wp-089](work-packages/wp-089-project-sidebar-and-dialog-frontend.md) | 0527 | WP-088 | Done (2026-08-30) | none — the live two-persona pass ran (consultant-01/consultant-02 on Tekos), 19/19 checks passed, no product defect |
| WP-090 | [wp-090](work-packages/wp-090-project-salesforce-quota-telemetry.md) | 0528 | WP-088 | Done (2026-08-30) | none on the repo-verifiable criteria — telemetry confirmed live 2026-08-29 and now regression-tested (2026-08-30), tool-policy decision recorded (no change); the live Salesforce three-cause pass is carved out to WP-101 (v0.7), no longer a precondition for this WP's or ADR-0528's closure |
| WP-091 | [wp-091](work-packages/wp-091-keycloak-client-reconciliation-and-zuno-admin-api.md) | 0530 | — | Done (2026-08-28) | none — reconcile ran live, `GET /api/colleagues` returns 200 |

## Phase 22: Day 0/Day 1 always-on infra reorg + AAP execution extension (added 2026-08-30, outside the original 40-ADR scope)

ADR-0418's own Context section (v0.4, still `Proposed`) had already named
a broader reshaping - collapsing Day 0 into a smaller "always-on infra"
tier (ArgoCD, Keycloak, Vault, External Secrets, AAP) - but deliberately
did not commit to it. ADR-0421 is that reserved decision, scoped
narrowly: `postgresql`/`keycloak`/`aap`/`aap-config` move from Day 1 into
Day 0 (right after `machines`); `nvidia-gpu`/`custom-metrics-
autoscaler`/`nfd`/`smtp` move from Day 0 into Day 1 in exchange. This
phase is the settled prerequisite for extending ADR-0418's AAP Job/
Workflow Template execution model to Day 2 and Day 3 - a Job Template's
`playbook` field is `day<N>_<verb>.yml`, so it needs each component's
final day-tier settled first. Follow-up WPs generalizing the Job Template
mechanism, adding Workflow Templates with parallelization, wiring
`make`'s `zuno_make_aap_mode` (local/remote/auto) routing, and tuning
`zuno-aap`'s resource requests/limits/quota from real load are briefed
separately and added to this phase's table as they land.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-093 | [wp-093](work-packages/wp-093-day0-day1-always-on-infra-reorg.md) | 0421 | — | Repo work merged (2026-08-30) - live verification pending | a full live `make d0 install all` → `make d1 install all` run against a real cluster, deferred pending explicit operator go-ahead (this repo's shared-cluster convention) |
| WP-094 | [wp-094](work-packages/wp-094-generalize-aap-job-templates.md) | 0418 | WP-093 | Done (2026-08-30) - live launch/Survey/credential round-trip confirmed (jobs 180/240/244) | install-type ClusterRole check deliberately deferred (low risk, not required to close Phase 1) |
| WP-095 | [wp-095](work-packages/wp-095-workflow-templates.md) | 0418 | WP-094 | Done (2026-08-30) - Day 1 `zuno-day1-check-workflow` full green run (job 243); Day 2 `zuno-day2-check-workflow` also full green (job 514, after WP-102) | none |
| WP-097 | [wp-097](work-packages/wp-097-make-aap-routing.md) | 0418 | WP-095 | Done (2026-08-30) - live-verified `zuno_make_aap_mode=auto` routing both Job and Workflow Template launches, exit-code propagation confirmed | `remote` unreachable-failure and `auto` fallback branches still only offline-tested (isolated shell-logic), not re-exercised live this pass |
| WP-098 | [wp-098](work-packages/wp-098-remove-aap-shipped-defaults.md) | 0354 | WP-073 | Repo work merged (2026-08-30) - live-verified: "Demo Project"/"Demo Job Template" confirmed gone from the Controller | none |
| WP-099 | [wp-099](work-packages/wp-099-aap-bootstrap-live-defects.md) | 0418 | WP-094, WP-095, WP-098 | Done (2026-08-30) - two consecutive `make d0 install aap-config` runs clean, all 5 defects found+fixed live | none |
| WP-102 | [wp-102](work-packages/wp-102-aap-execution-environment-kustomize.md) | 0418 | WP-095 | Done (2026-08-30) - custom EE (`kustomize`/`oc`/`cosign`) built, registered, assigned; full green `zuno-day2-check-workflow` (job 514) | none |
| WP-103 | [wp-103](work-packages/wp-103-aap-launch-rbac.md) | 0418 | WP-094, WP-097 | Done | model Controller RBAC for who may launch which Job/Workflow Template, ahead of Phase 3/4 (renumbered from a WP-101 draft that collided with the pre-existing Salesforce WP-101) |
| WP-105 | [wp-105](work-packages/wp-105-node-failover-drill-scenario-command.md) | 0536 | WP-094, WP-095, WP-097, WP-103, WP-087 | Done | Both Part A (local path) and Part B (AAP Workflow Template with a real human approval click in the Controller UI) live-verified end to end 2026-08-30/31 - Comage fails over `local-wesh`→`local-qwen35`→`local-wesh` both ways, Tekos unaffected throughout; 6 real bugs found and fixed live across both parts (see WP-105/evidence doc) |

## Phase 23: qwen3.5-9b fleet default + extended OVHcloud reasoning access (added 2026-08-30, outside the original 40-ADR scope)

ADR-0531 promotes `qwen3.5-9b` (ADR-0526/WP-087's training base, live per WP-092) from an
unreferenced provider to the fleet-wide default, pairs it with Comage's `qwen3.5-9b-wesh`
fine-tune for both Tekos and Comage, and extends `ovhcloud-gpt-oss-120b` reasoning-task access
(ADR-0416's mechanism, previously Arkos-only) to Tekos's and Comage's own reflexional tasks.
Config-content only - no code change, no new provider, no infrastructure impact.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-096 | [wp-096](work-packages/wp-096-qwen35-9b-fleet-default-and-ovh-reasoning-rollout.md) | 0531 | WP-087, WP-092 | Done (live-verified 2026-08-30 — ai-gateway rebuilt/rolled out, routing checks confirmed live, zero model-pod churn) | none |

## Phase 24: knowledge.tech per-source ingestion cadence split (added 2026-08-30, outside the original 40-ADR scope)

WP-22 implemented ADR-0105's per-source cadence decision at domain
granularity only; `knowledge.tech`'s two independent sources
(fetch-redhat, fetch-confluence) still shared one schedule and one KFP
pipeline. WP-100 gives them independent schedules (redhat weekly,
confluence every 6h) and closes the detect-changes/changeset
concurrency-isolation gap that independent scheduling exposed, without
splitting `knowledge.tech`'s single database (ADR-0202).

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-100 | [wp-100](work-packages/wp-100-split-tech-ingestion-cadence-by-source.md) | 0105 (amended) | WP-22 | Done (2026-08-30) | none |

## Phase 25: Salesforce sandbox credential provisioning (added 2026-08-30, v0.7)

Three separate "blocked on sandbox" notes — WP-22/ADR-0218's `fetch-salesforce`
batch-ingestion cadence, WP-33's deferred live Comage gate, and ADR-0528/WP-090's
deferred live three-cause pass — shared one root cause with no owning WP: no
Salesforce org, sandbox or otherwise, has ever existed in this cluster
(`ansible/roles/vault/tasks/install.yml` only seeds `zuno/salesforce/technical`
when real credentials replace its `xxxxxx` placeholder). WP-101 is that owner:
a pure operator/infra provisioning task with zero anticipated application code
changes, closing all three notes' live-pass follow-ups once a credential exists.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-101 | [wp-101](work-packages/wp-101-salesforce-sandbox-credentials.md) | 0512, 0528, 0218 | none | Not started (2026-08-30) | obtain a sandbox org + technical-user token, seed Vault, restore the salesforce-mcp install path, then run the three confirming live passes (Comage freshness read/write, ADR-0528's 404/403/503 causes, `fetch-salesforce` one-shot KFP run) |

## Phase 26: adopt RHTAS as the artifact trust service (added 2026-08-30, v0.9; split into WP-110/WP-111 2026-09-02)

ADR-0535 supersedes ADR-0420, replacing Phase 12's in-cluster Vault Transit
signing with RHTAS (self-hosted Fulcio/Rekor/Policy Controller) as a
product-demonstration decision, not a security-driven one — Vault Transit
stays technically sufficient and cheaper for the signing problem alone.
This was originally scoped as one WP (WP-104); it is now split in two,
following this platform's Day 1/Day 2 convention, so operator+fundamentals
and the actual cutover can each be committed/pushed/tested live
independently — WP-110 (Day 1 `rhtas`) deploys the RHTAS operator, decides
and implements its namespace/Trillian-storage/Keycloak-OIDC-identity design
(now settled in ADR-0535 itself rather than left open), and proves a
keyless test signature end to end. WP-111 (Day 2 `rhtas-config`) assesses
whether RHOAI needs any direct integration (expected "not applicable for
v0.9" per ADR-0535's AI/model Non-goal, confirmed live rather than assumed)
and then cuts the same 14 first-party images Phase 12/WP-070 already signs
over to RHTAS/Cosign keyless signing, deploying the Sigstore Policy
Controller in audit-only mode. OKF bundle signing (blocked on
ADR-0506/ADR-0507), AI/model artifact signing beyond WP-111's assessment,
and admission-enforcement (reject mode) are explicitly deferred to later
ADRs/WPs — see ADR-0535's Non-goals and Migration/evolution sections.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-104 | [wp-104](work-packages/wp-104-rhtas-deployment-and-image-signing-cutover.md) | 0535 | none | Cancelled — superseded by WP-110/WP-111 (2026-09-02) | none |
| WP-110 | [wp-110](work-packages/wp-110-rhtas-operator-deployment-and-fundamentals.md) | 0535 | none | Done (2026-09-02) | none — operator v1.4.3 live, Securesign Ready (Trillian on the shared MariaDB), `zuno-signer` OIDC identity live-tested, keyless sign+verify smoke test passed with a real Rekor entry; 5 live findings fixed in-repo, see the brief's State |
| WP-111 | [wp-111](work-packages/wp-111-rhtas-config-rhoai-assessment-and-signing-cutover.md) | 0535 | WP-110 | Done (2026-09-02) | none — signing cutover fully live-verified (`make d2 check supply-chain` PASS, 14 images, real Rekor entries); Policy Controller deployed but dormant (zero namespaces scoped - its digest-format gate isn't governed by mode:warn and this repo's `tag: latest` convention, ADR-0115 gap 2, made it reject real pods) until that gap closes; Part A found the ADR-0535 Non-goal premise already stale (WP-087) |

## Phase 27: RHOAI HardwareProfiles + MaaS ExternalModels for mistral/gpt-oss-120b (added 2026-09-01, v0.5)

A live diagnostic of `granite-7b-redhat-lab`'s `CrashLoopBackOff` found its
Dashboard-assigned `HardwareProfile` declares no GPU identifier at all - a
hardware-profile authoring gap, not a cluster GPU shortage. The same session
found `mistral` and `gpt-oss-120b` are the only two chat providers still
called directly from `ai-gateway`, bypassing the MaaS governance plane every
local model has used since ADR-0521, even though the transport code for a
SaaS candidate already exists (`maas_adapter.py`'s dormant
`MAAS_EXTERNAL_EGRESS_ENABLED` gate). ADR-0537/WP-106 close both gaps: two
new GitOps-managed `HardwareProfile` CRs for the two MIG tiers already in
use, hardware-profile annotations on the five existing local model
InferenceServices, and `ExternalModel`+`MaaSModelRef` publication for
`mistral-large-latest` (kept on its native `api.mistral.ai` API) and
`gpt-oss-120b` (OVHcloud, unchanged endpoint) with per-group
`MaaSSubscription` quotas, followed by a full cutover of `ai-gateway`'s
routing to the MaaS path for these two providers.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-106 | [wp-106](work-packages/wp-106-rhoai-hardware-profiles-and-maas-external-models.md) | 0537 | WP-27, WP-076 | Not started (2026-09-01) | apply the two `HardwareProfile` CRs and confirm Dashboard parity with Granite; live-verify the `ExternalModel`/`MaaSModelRef` route-identity behavior (unknown for this backend kind); live-verify the full `ai-gateway` cutover (per-group quotas, Finage still denied) before removing the old direct-call code paths |

## Phase 28: TrustyAI evaluation and guardrails (added 2026-09-02, v0.7)

ADR-0534 extends the platform's existing TrustyAI integration (already
`Managed` on the `DataScienceCluster`, already serving ADR-0108/WP-10's
LM-Eval model-benchmarking gate) from benchmarking-only to evaluation and
guardrails. TrustyAI is not a new operator - it already lives inside the
Day 1 `openshift-ai` component - so there is no operator install step here.
A new Day 2 component, `trustyai-config` (mirroring the `aap`/`aap-config`
and `lightspeed`/`lightspeed-config` split), carries the new work: generic
RAGAS/Garak evaluation frameworks, the `mcpGuardrailsMode` flip, and
guardrail hooks wired to Agent Runtime's boundary so the full agent
exchange (RAG context, MCP tool use, final response) can be evaluated.
Guardrail enforcement starts observe/log-only; a move to blocking
enforcement is a later, separate decision. Three WPs execute this, one per
ADR-0534 phase, with Phase 3 (PEFT/LoRA model comparison) folded into the
third rather than split into its own WP.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-107 | [wp-107](work-packages/wp-107-trustyai-baseline-verification-and-config-scaffold.md) | 0534 | WP-10 | Done (2026-09-02) | none — all three checks ran live: `TrustyAIReady=True`, scaffold Synced/Healthy via `make d2/d3`, and a fresh `Complete/Succeeded` LMEvalJob run (acc=0.72) after fixing the double-TLS regression the check itself uncovered (commit `690892da`) |
| WP-108 | [wp-108](work-packages/wp-108-trustyai-ragas-garak-guardrails-enablement.md) | 0534 | WP-107 | Done (2026-09-02) | none — flip executed, proven to kill the LMEvalJob controller, reverted (ADR-0534 amended); Garak smoke `succeeded=1` with report, built-in detector Ready with a live PII detection (score 1.0), RAGAS gap recorded and moved to WP-109 |
| WP-109 | [wp-109](work-packages/wp-109-trustyai-zuno-stack-integration-and-model-comparison.md) | 0534 | WP-108 | Done (2026-09-02) | none live — all proven: observe-only guardrails on real traffic (PII+jailbreak flagged, responses unmodified), RAGAS `faithfulness=1.0` on real retrievals, reproducible Garak MitigationBypass 100% ASR on wesh, and a real regression FAIL (`-0.12`) from the PEFT gate; remaining follow-ups (wire the FAIL into ADR-0107's promotion gate, garak-into-artifact folding, base-model garak rerun) are recorded in the brief's Out of scope |
| WP-113 | [wp-113](work-packages/wp-113-trustyai-observability-dashboard.md) | 0534 | WP-109 | Done (2026-09-02) | none — born from the FAILED human live test of ADR-0534 and closed the same day WITH the human sign-off: guardrails/RAGAS/Garak metrics live in Thanos via the standard OTLP pipeline, `zuno-trustyai` Grafana dashboard applied and human-validated (after one adjustment round: kube-state lag + missing table-merge transform); the RHOAI "Model monitoring bias" card stays unconfigured by decision |
| WP-114 | [wp-114](work-packages/wp-114-peft-regression-gate-input-and-trustyai-alerting.md) | 0107, 0534 | WP-109, WP-113 | Done (2026-09-02) | none — the gate's fourth input is live (real FAIL on wesh's -0.12, then operator-granted waiver: PASS with the regression visibly WAIVED, future candidates still gated at 0.05), and `GarakAttackSuccessHigh` is FIRING on the real MitigationBypass finding; the one remaining proof deferred by decision is a full-gate run with a real mlops candidate |

## Phase 29: retry design for skipped tool calls on the single-shot graph shape (added 2026-09-02, v0.9)

ADR-0516 shipped with no work package (`docs/roadmap/evidence/
adr-0516-diagram-render.md`'s own header) and its "Accepted risks" section
already named the `generate_image` tool-schema wording ambiguity as
"speculative ahead of real usage data." A 2026-09-02 day2-stresstest deep
dive produced that data: fixing the schema/prompt contradiction itself
(commit `ef7b5c43`) left a second, deeper problem live-confirmed - Comage's
reference model (`qwen3.5-9b-wesh`, already documented in ADR-0526 as
prone to narrating tool use) now correctly identifies `generate_image` as
the right tool for a "mockup" request but narrates calling it instead of
actually invoking it. `reason_node`
(`components/agent-runtime/app/graph/nodes.py:791-938`), the single
decision point on the `retrieve_reason_respond` graph shape, has no retry
for a skipped tool call the way `_resolve_diagram_generation_call` retries
a failed render - fixing that changes shared runtime behavior across every
agent on that shape, so WP-112 designed the fix before merging one blind.
Design done and judged in-scope for the same WP (bounded, mirrors an
existing precedent); implemented and unit-tested 2026-09-02, live
verification against a real `qwen3.5-9b-wesh` deployment still open.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-112 | [wp-112](work-packages/wp-112-comage-tool-call-narration-retry.md) | 0516 | none | Repo work merged (2026-09-03) | second fix landed 2026-09-03: `_NARRATED_TOOL_NAME_PATTERN`'s name-substring trigger misses narration that describes the tool without naming it (live French example), so rather than widen that shared-runtime trigger blind, Comage's system prompt gained the explicit "actually invoke it" instruction that closed the identical defect for Arkos (`b0cb07f4`). Comage-only. Needs rebuild + `make d3 sign agents` + `make d3 stresstest agents BULK=0` to confirm both named checks green with `sxa_visualization_boundary` still passing; widening the trigger stays the designed next step if it does not close |

## Phase 30: RHOAI 3.5 dashboard workload surfaces (added 2026-09-02, v0.7)

Closing ADR-0534's visibility work exposed a broader gap: four RHOAI
dashboard pages sit empty or unconfigured on this platform. Evaluations
(ADR-0534's own surface) needed the dashboard's hidden feature flags plus
an `EvalHub` instance - the operator's controller had run since install
with nothing to reconcile. The other three are ADR-0538: the
`mlflowoperator` component has been `Managed` for weeks with no `MLflow`
CR, leaving the LoRA pipeline's run history as S3 JSON with no comparison
surface; Kueue has been installed and reconciling since ADR-0321 without
ever admitting a single workload (no namespace opted in, and its own
"GPU flavor before queued workloads" precondition was never discharged);
and the TrainJobs/Ray controllers run unused by design, since ADR-0302
keeps KFP as the pipeline orchestrator. The decisions taken: stand MLflow
up PostgreSQL-backed and log to it non-fatally (backfilling the one
existing green LoRA run rather than re-spending GPU), complete Kueue's
GPU-MIG quota and make the TrustyAI evaluation Jobs its first opt-in
consumers, and enable the training-jobs UI while explicitly deferring any
move off KFP to a later ADR.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-115 | [wp-115](work-packages/wp-115-trustyai-dashboard-ui-flags-and-evalhub.md) | 0534 | WP-113 | Operator pending (2026-09-02) | flags patched live and the `EvalHub` instance is `Ready` (commits `932abb63`, `365c8252` — the ea build crashes on any `otel` block); remaining: one real UI-launched evaluation run end to end |
| WP-116 | [wp-116](work-packages/wp-116-mlflow-experiment-tracking.md) | 0538, 0302, 0526 | ADR-0529 pguser mechanics | Done (2026-09-03) | none — MLflow runs PostgreSQL-backed (`Available=True`, 50 tables migrated), the pipeline logs params/metrics/gate non-fatally, and the `wesh-20260829-145123` run is visible in the Experiments page via an idempotent backfill; seven live defects fixed on the way in, including a wedged `zuno` database that had been blocking `make d0 install postgresql` for everyone |
| WP-117 | [wp-117](work-packages/wp-117-kueue-gpu-quota-and-queued-workloads.md) | 0538, 0321, 0351 | WP-109, WP-113 | Done (2026-09-03) | GPU-MIG ResourceFlavor + ClusterQueue quota, LocalQueue and namespace enrolment for `zuno-ai-run`, queue-name labels on the garak/ragas Jobs, plus the `trainingJobs` dashboard flag. Second attempt: enrolment is namespace-wide with no per-workload opt-out, so the recreation loop had to be closed first; quota deliberately undersized to serialise multi-GiB image pulls |
| WP-118 | [wp-118](work-packages/wp-118-demo333-portability-blockers.md) | 0517 | none | Done (9 of 9 blockers closed, 2026-09-03) | no chart default names `demo222` any more: five charts on the `apps.mycluster.example.com` token, the `machines` chart's AWS identity, four `gp3-csi` defaults and cert-manager's Route53 zone all discovered at apply time and flipped to placeholders, the Salesforce duplicate-seed and MariaDB S3 gaps closed, and the RHOAI InstallPlan drift closed as a decision (keep the gate, detect it in precheck from the PackageManifest). Every flip was applied live first and verified inert — MachineSets byte-unchanged, all 15 PVCs unchanged, ACME track intact, cert-manager's re-apply `changed=0`. Closes the known literals, not the claim that the automation bootstraps: that is ADR-0517's own run, still blocked on `demo333` being provisioned |
| WP-119 | [wp-119](work-packages/wp-119-kfp-submitted-trainjob.md) | 0539, 0538, 0302 | none | Repo work merged (2026-09-02) | the LoRA step now submits a Kubeflow `TrainJob` and waits, moving GPU placement out of a compiled DAG into a namespaced `TrainingRuntime` in Git; KFP still owns the DAG and the ADR-0107 gate. Shipped disabled - remaining is the scale-from-zero probe for a JobSet-owned pod, then enabling it for a real run |
| WP-120 | [wp-120](work-packages/wp-120-guardrail-policy-as-nemo-rails.md) | 0540, 0534 | WP-108, WP-113 | Repo work merged (2026-09-03), deployed and live | guardrail policy moved out of the agent-runtime image into a Helm-rendered ConfigMap a `NemoGuardrails` server mounts; same eight detection classes, still observe-only, and the rails really do cost no inference - `llm_calls_count: 0` live. Backend flipped to `nemo`, dashboard panel widened with it. The five-point gate caught four SILENT defects that would each have produced a permanently-empty detections series at HTTP 200: the ConfigMap key must be `config.yaml`, `config_id`/`options` nest under `guardrails` (dropped, not rejected, at the top level), `model` is required, and the activation log sits one level deeper than assumed. The ~14GB image pull also has to be kept off the control-plane nodes - unconstrained, the first attempt evicted 43 pods. ADR-0540 Decision 4 is amended: `DETECTOR_PARAMS` stays as long as the `GuardrailsOrchestrator` is the declared fallback, because it is that fallback's only policy and deleting it would make the rollback a detector that finds nothing. Outstanding: no authenticated end-to-end turn driven since the flip |
| WP-121 | [wp-121](work-packages/wp-121-llminferenceservice-spec-scaling.md) | 0542, 0318 | none | Done (2026-09-03) | first autoscaling object on the platform, via `LLMInferenceService.spec.scaling` on `gpt-oss-20b` at min=max=1 - not a raw `VariantAutoscaling`, which the operand deprecates. `maxReplicas` stays 1 because the GPU ResourceQuota is saturated (3/3, 2/2) and no model can reach two replicas; raising it is a separate approval. Live-verified: ScaledObject Ready=True/Active=True and `keda-hpa-gpt-oss-20b-kserve-keda` created with ValidMetricFound; the controller honoured `spec.scaling` (Deployment spec.replicas=1). The real gap was the `ClusterTriggerAuthentication` RHOAI names in `inferenceservice-config` and does not ship - without it the ScaledObject sat Ready=False for 9h with no HPA at all |
| WP-123 | [wp-123](work-packages/wp-123-dashboard-feature-flags-in-ansible.md) | 0538, 0534, 0517 | none | Repo work merged (2026-09-03), not yet applied live | the four RHOAI dashboard feature flags had no applier at all: `disableLMEval`/`guardrails`/`trainingJobs` were posted by a human with `oc patch` and recorded only in prose, so a from-scratch install came up with those surfaces silently gone - a tenth blocker of WP-118's class, invisible to it because a cluster-only mutation leaves nothing in the repo to grep for. They now live in `ansible/roles/openshift_ai/tasks/set_dashboard_flags.yml` and are applied as a partial merge patch shared by install and reconcile, with a `check` tripwire that prints `ABSENT` distinctly from `=false`. Not GitOps, deliberately and for a reason read off `managedFields`: the CR has three concurrent writers, the dashboard UI itself being one of them. A fourth flag was missing entirely - `disableKueue: false` - which is what surfaced this: the dashboard's flag evaluator returns "off" for an UNDEFINED key BEFORE applying the `disable*` inversion, so an absent key is a disabled feature, not a default. That showed as "Kueue is disabled in this cluster" in `zuno-ai-run`, with Deploy model disabled and an empty hardware-profile list, while the Kueue operand was Available with two ready replicas. ADR-0538 decision 5 amended: mechanism only, the not-GitOps posture is unchanged and reinforced. Outstanding: the live `make d1 reconcile openshift-ai` pass and the dashboard re-check, verification steps 2-7 of the brief |

## Phase 31: MaaS gateway's missing Keycloak identity source (added 2026-09-03, v0.7)

Live-verified while confirming WP-112's Comage fix: every real persona
token gets a `401` from the local-model MaaS providers
(`local-wesh-maas`/`local-gpt-oss-maas`/`local-maas`), across multiple
agents, with no visible user-facing error (the provider-fallback chain
masks it). Root cause confirmed live via a discriminating generation-bump
test: `maas-controller` (the same operator ADR-0537 already found one
confirmed defect in) never adds a Keycloak/OIDC identity source to the
`maas-gateway-auth` AuthPolicy no matter what `ModelsAsService.spec.
externalOIDC` says, despite the AuthPolicy self-reporting `Accepted`/
`Enforced` - the exact "green lies" shape ADR-0511/WP-54 already found
once for Kuadrant. Net effect: ADR-0521's per-group `MaaSSubscription`
quota governance is not currently exercised by any real traffic.
Live-confirmed 2026-09-03, closed with a clean end-to-end pass: the
`Tenant` CRD's own `spec.externalOIDC` (not `ModelsAsService`'s) was the
real fix - patched on the operator-managed `Tenant`, `maas-gateway-auth`
gained a real Keycloak identity source within 14 seconds, clearing the
401. A second cause then surfaced: Keycloak's `groups` claim carries full
paths (`/agent_tekos`) that `maas.models[].subscriptions[].group` in
`values.yaml` declared without the leading slash - fixed there (commit
`9439e3b8`), zero blast radius elsewhere. A third suspected cause
(`maas-api` cache staleness) prompted an approved `maas-api` rollout
restart that turned out unnecessary - the real remaining issue was this
WP's own manual test sending the wrong `model` identifier
(`app/maas_adapter.py` sends the MaaS-published name, not the raw KServe
name the URL path uses). Corrected, a fresh persona token now gets a
genuine `200` with real model output through `local-wesh-maas`.

| WP | Brief | ADRs | Depends on | State | Operator actions remaining |
|---|---|---|---|---|---|
| WP-122 | [wp-122](work-packages/wp-122-maas-gateway-keycloak-identity-gap.md) | 0537, 0521 | none | Repo work merged (2026-09-03) | none - closed with a clean live pass; the downstream re-run it unblocked (WP-112's `img-mockup_request`/security-check) surfaced a separate, real WP-112 gap, tracked there, not here |

## Cross-version carve-outs

ADRs whose version tag and completing phase differ — expected, dependency-
driven:

| ADR | Version | Completes in | Why |
|---|---|---|---|
| 0105, 0109, 0110 | v0.1 | Phase 2 (WP-22, WP-24, WP-25) | need the 0202 metadata schema / 0205 freshness policy / multi-domain core to avoid building twice |
| 0209 | v0.2 | Phase 3 (WP-31) | its cross-agent acceptance scenario needs Arkos |
| 0342 | v0.3 | WP-30 mechanism + WP-31 proof | Arkos is the second shape |
| 0350 (ex-0113) | v0.3 | Phase 3 (WP-38) | its own deferral: 0326 (agents) → 0327 (contract) → 0308 (operator) |

## Dependency graph

```text
WP-00 ─┬─ WP-01 ─┬─ WP-02 ──────────────┬─ WP-25
       │         ├─ WP-26 ─────────┐    │
       │         └─ WP-32 ────┐    │    │
       ├─ WP-03 ── WP-27      │    │    │
       ├─ WP-04 ── WP-05      │    │    │
       ├─ WP-06               │    │    │
       ├─ WP-07 ──────── WP-22│    │    │
       ├─ WP-08  WP-09        │    │    │
       ├─ WP-10 ──────────────│────│────│────────── WP-34 ─┬─ WP-39
       ├─ WP-11  WP-12  WP-13 │    │    │              ▲   └─ WP-40 ── WP-42
       └─ WP-20 ── WP-21 ─┬─ WP-22 ── WP-23 ── WP-33 ──┘       ▲
                          ├─ WP-24 ───────▲     ▲  │        (WP-09)
                          └─ WP-28 ─┐     │     │  └─ WP-35 ── WP-36 ──┐
                                    │     │     │                      │
                 WP-30 ─────────── WP-31 ─┴─────┘                      │
                                    │                                  │
                                    └─ WP-37 ── WP-38 ── WP-41 ◄───────┘
```

Note (2026-08-20): the WP-22 → WP-23 → WP-33 → WP-35 arrows above represent
only the fixture-driven adapter dependency, satisfied since 2026-08-15 —
not a current blocker. WP-22's own remaining scope (recurring-cadence
automation, real Salesforce credentials — now WP-101, v0.7 per ADR-0218;
Aramis is gone from the repo entirely) blocks nothing downstream today.

## Operator action queue

Consolidated human/cluster steps, roughly in the order they are needed.
Acquiring credentials ahead of the WP that needs them keeps the pipeline
moving.

1. **Commit the ADR-0344 change set** (pre-existing dirty working tree) so
   WP commits stay clean.
2. ~~Quay + GitHub Actions credentials → one real release (WP-04 stage 2)~~
   **No longer queued (2026-08-22):** that release already ran for real
   (`v0.1.0`, run `32273454405`); WP-04/ADR-0115 is now closed — deferred,
   so tag pinning, blocking lint gates, `targetRevision` pinning and the
   immutable-tag rule stay parked until a future ADR reactivates this
   stream.
3. **Confluence Cloud API token** at Vault `zuno/confluence/technical`
   (WP-02); later real space keys/directories (WP-07) and an ACL check
   (WP-25).
4. **Cluster time for Day 0/1 verification**: OGX DSC reconcile + corpus
   proof (WP-06); rag-ingestion KFP/DSPA verification (WP-07); per-domain DB
   provisioning (WP-21); realm re-apply (WP-32).
5. **MaaS environment**: adapter comparison (WP-03), governance verification
   (WP-27).
6. **Salesforce (sandbox) credentials** — now tracked as its own WP
   ([WP-101](work-packages/wp-101-salesforce-sandbox-credentials.md),
   v0.7, added 2026-08-30): unblocks WP-33's deferred live Comage gate,
   ADR-0528/WP-090's deferred live three-cause pass, and ADR-0218's
   `fetch-salesforce` batch-ingestion cadence — three separate "blocked on
   sandbox" notes that had no single owner until now.
   **SXA teardown + one re-ingestion** (WP-084 — ADR-0219
   retired the deterministic SXA path, so WP-23's "approved dump snapshot"
   action is closed by removal; what remains is reclaiming the retired
   MariaDB/Postgres/Vault resources and one full re-embed of
   `rag-sxa-legacy` under the new `sxa-dump://` doc ids);
   **Google Workspace delegated-flow verification** (WP-26 optional, WP-31,
   WP-33).
7. **GPU cluster time**: LM-Eval (WP-10), MLOps pipeline + adapter promotion
   (WP-34), dynamic LoRA (WP-39), benchmarking loop (WP-40).
8. **Drills**: failover + SLO measurement (WP-12), restore drill (WP-13),
   SecNumCloud cluster-side verification (WP-11).
9. **Evaluation scenario reviews** (human, per agent): WP-31, WP-33, WP-35,
   WP-36 done 2026-08-30 (retroactive catch-up); WP-41 — required before
   each cluster gate run counts.
10. **Autonomy sign-off** after one observed cycle (WP-42).

## Change log

- 2026-08-27 — ADR-0527 and ADR-0528 authored, superseding ADR-0213 in full
  and ADR-0512's clause 3 respectively: the project becomes a first-class
  object owning every `project_id`, carrying a four-role RBAC over subjects
  and business-role groups, a 54 000-character engagement context injected as
  budgeted background, and an optional Salesforce link that makes it *customer*
  or *free*; quota and telemetry re-key onto the Zuno `project_id`. WP-088/089/
  090 briefed as Phase 21; WP-066 abandoned.
- 2026-08-26 — ADR-0219 authored, superseding ADR-0216 and ADR-0217 in full
  and ADR-0016 in full: SXA is the company's pre-2021 commercial record with
  no live system behind it, so the deterministic MCP path and the duplicate
  `knowledge.sxa` domain are retired rather than completed. Phases 9 and 11
  close as Abandoned; Phase 14 / WP-084 carries the removal. Both superseded
  ADRs were renamed and rewritten to drop "anonymized" from their filenames,
  titles and bodies — `sxa_anonymize.py` was deleted on 2026-08-23 and no
  path has anonymized SXA content since, so the language asserted a property
  the code does not have. A deliberate exception to ADR immutability.
- 2026-08-26 — ADR-0218 amended and enacted, `Proposed` -> `Implemented`,
  target `Unscheduled (backlog)` -> `v0.7`. Its original clause 1 dropped
  Aramis as a tracked objective while deliberately leaving the code inert in
  the tree; that is reversed — `fetch-aramis` and all its wiring are now
  removed from `components/rag-ingestion/`, `gitops/charts/rag-ingestion/`,
  `ansible/` (the `zuno/aramis/technical` Vault seed), `knowledge/`,
  `platform/bindings/knowledge/bindings.yaml` and `agents/advantage/`'s OKF
  prose. `knowledge.adv` is retained with no ingestion adapter (descriptor,
  policy entry and `rag-adv` binding intact) — its sourcing stays an open
  decision for the Advantage slice, per that ADR's own out-of-scope clause.
  The Salesforce half is deferred, not dropped: `fetch-salesforce` and
  `domains.sales` (`enabled: false`) stay in the tree and their cadence lands
  in v0.7 alongside ADR-0105/0206/0213's reprioritization group. Deploy-side
  no-op — `domains.adv` was never enabled and the Aramis Vault path was never
  populated. "Aramis access" removed from the operator action queue.
- 2026-08-25 — WP-073 `aap-config` live-verified Done on demo222: `zuno`
  organization, Project/Inventory/Credential/Job Template CRs reconciled,
  Keycloak SSO authenticator wired (API-confirmed; browser login flow
  still unverified), a manual `zuno-day0-check` job ran successfully and
  a second install was idempotent. Five bugs fixed en route: AAP 2.5+
  gateway-level organizations/tokens (`56c48fe`), wrong connection-secret
  host (`56c48fe`), missing `zuno_repo_root` under AAP's group_vars-less
  inventory (`eef095f`), `cluster-reader` excluding Secrets for vault's
  precheck (`c664273`), and a genuine pre-existing, AAP-unrelated bug — a
  stale `ClusterRoleBinding` name in `openshift_rbac_groups`'s precheck
  from the ADR-0349 rename, false-negative on every cluster (`c3417a9`).
  One limitation accepted by design: vault's precheck needs `pods/exec`,
  absent from `cluster-reader` and deliberately not widened — `vault`
  always reports "NOT installed" through this Job Template; `make d0
  check vault` from an operator shell stays authoritative. WP-072 also
  flipped to `Done` (was left at "Repo work merged" despite being
  live-verified the same day).
- 2026-08-24 (evening) — WP-072 repo work merged: `aap` role/chart/apps,
  four dedicated Crunchy databases, Vault seeds, Keycloak OIDC client. A
  live CRD inventory against `demo222` (operator subscribed, `oc explain`
  run, then torn back down before any `AnsibleAutomationPlatform` CR was
  created) resolved ADR-0354 clause 5's open question - Gateway/
  Controller/Hub/EDA each need their own database secret, not one shared
  one - and confirmed WP-073 can use Path A (the platform operator's own
  bundle already ships the Project/JobTemplate CRDs). Live install
  (`make d1 install aap`) not yet run.
- 2026-08-24 (evening) — Phase 13 added: ADR-0354 (Ansible Automation
  Platform, amended Day 0 → Day 1 placement and retargeted v0.3 → v0.2)
  and new companion ADR-0355 (`mcp-aap`, v0.3) briefed as WP-072/WP-073/
  WP-074. No repo code changes yet — documentation phase only.
- 2026-08-24 (evening) — WP-071 landed and closed `Done`: the "upstream
  Kuadrant wasm-shim defect" blocking WP-27 (this file) and WP-54
  ([okf-roadmap.md](okf-roadmap.md)) is retracted — root-caused to a
  locally-fixable Authorino/Envoy TLS trust mismatch, plus a second gap
  specific to `zuno-agent-gateway` (Kuadrant's own generated `EnvoyFilter`
  never adds TLS to the ext_authz cluster; `maas-default-gateway` only
  worked via a separate RHOAI-owned filter). Both fixed and live-verified
  on both gateways. See ADR-0201/ADR-0511's 2026-08-24 implementation
  notes and `docs/roadmap/work-packages/
  wp-071-authorino-service-ca-trust-alignment.md`. WP-27's row above
  updated; no new WP-tracker table added here (ADR-0201 is v0.5-targeted —
  same "README index only" convention as v0.4+, see the afternoon entry
  below).
- 2026-08-24 (afternoon) — three new platform version bands created:
  v0.5 (make the MaaS governance plane live and used by agents), v0.6
  (prove platform automation via a from-scratch redeploy on a new
  cluster), v0.7 (GitHub-Actions-based release automation). ADR-0201/
  ADR-0511/ADR-0512 move a second time today, out of this file's v0.3
  scope and into the new v0.5 milestone (same root blocker as the
  morning's move, better-scoped home). ADR-0115 moves out of v0.1 into
  v0.7, alongside WP-04's GitHub Actions release-pipeline scope. New
  small ADR-0517 authored for v0.6 (demo333 cluster redeploy).
  `docs/adr/README.md` (sole status authority) and `versions.md` updated
  with new `## version 0.5/0.6/0.7` sections; no new WP-tracker table
  added for these bands, matching how v0.4 is tracked today (README
  index only).
- 2026-08-24 (morning) — ADR-0201 (MaaS governance, v0.2 -> v0.3) and
  ADR-0511/ADR-0512 (OKF quota policy + project-bound tasks, OKF v0.1 ->
  v0.3) retargeted: WP-27 and WP-54 both stalled on the same upstream
  Kuadrant wasm-shim defect with no repo-side fix, and ADR-0512/WP-55 has
  a hard `Depends on: WP-54`, so all three move together rather than
  sitting blocked inside their original milestones. `docs/adr/README.md`
  updated (sole status authority); WP rows/Tracker/dependency-graph
  entries for WP-27/WP-54/WP-55 are unchanged — they track WP state, not
  version. (Superseded same day — see the afternoon entry above.)
- 2026-08-23 — ADR-0218 authored (Aramis will never be provisioned as a
  service; Salesforce's batch RAG-ingestion cadence deferred to an
  unscheduled backlog, not v0.4 — that band has no ingestion-adapter slot),
  superseding the Salesforce/Aramis clauses of ADR-0105 and ADR-0204 part 2;
  WP-22's tracker row and brief updated accordingly. Same pass: WP-28's
  tracker row corrected from `Repo work merged` to `Done` — it had not been
  updated when WP-31's 2026-08-15 merge already closed ADR-0209.
- 2026-08-22 — WP-070 closed out: `make d2 check supply-chain` (new
  `ansible/roles/supply_chain`) automates image-signature verification in
  the same "resolve outside, verify inside" split as the signing Jobs;
  live-verified both a clean PASS (13/13) and a deliberately tampered
  digest correctly failing. `build-publish.yml` kept (not retired) - three
  stale doc references to the removed keyless mechanism fixed instead.
  Found and fixed 3 more real bugs (mirrored-image false positives,
  agent-bff/agent-frontend invisible to the scan, cosign verify's TUF
  cache write failure) plus a recurring ansible Job-log-display bug across
  all three signing/verify Jobs. ADR-0420 -> Implemented.
- 2026-08-22 — ADR-0420 authored (replace ADR-0106/ADR-0115's GitHub-OIDC/
  Fulcio/Rekor keyless signing with an in-cluster Vault Transit backend);
  WP-068/069/070 briefed. WP-068/069 Done, live-verified - all 8 OKF
  bundles signed and `requireSignedBundles: true` flipped and rolled out
  on the real Deployment. WP-070 repo work merged and live-verified: all
  14 first-party images signed and independently confirmed with
  `cosign verify --key`, one near-miss (an early `ai-gateway` signing
  failure) caught only by a final all-images sweep, not by a green
  playbook run. Several real Vault-policy/RBAC/TLS/registry-auth bugs
  found and fixed along the way (see ADR-0420's own implementation notes).
- 2026-08-20 — ADR-0215 authored (Tekos chat had no cross-turn memory: each
  model call carried only the newest message, not prior turns); WP-060
  briefed to carry history into every agent's prompts with a token budget
  and Claude-Code-like compaction.
- 2026-08-18 — WP numbering continues at WP-43 in the
  [OKF stream roadmap](okf-roadmap.md) (ADR-0501); this roadmap's own scope
  closed with WP-42 and gains no further WPs.
- 2026-08-14 — roadmap created; WP-00 executed (check_docs.py baseline
  fixed: renamed roadmap-container entries in `ADR_INDEX_EXCLUDED_FILES`,
  status normalization for ` - see …` / linked-Superseded forms); 35 briefs
  authored (13 + 9 + 13).
