# AIAgent Operator

The operator will reconcile `AIAgent` custom resources into agent-specific OpenShift resources and references to shared platform services. The initial repository stage records the contract only; controller implementation follows the ADR-defined MVP sequence.
