# TEST-ONLY FIXTURE. This is NOT the real External Secrets Operator CRD -
# that ships via gitops/charts/external-secrets, a Day 0 cluster
# prerequisite, and is never applied by this operator. It is a minimal,
# permissive stand-in so internal/controller/suite_test.go's envtest
# suite can exercise the reconciler's real ExternalSecret-creation code
# path without needing a live cluster with External Secrets installed.
# Never deploy this file for real - see operator/aiagent-operator/README.md.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: externalsecrets.external-secrets.io
spec:
  group: external-secrets.io
  names:
    kind: ExternalSecret
    listKind: ExternalSecretList
    plural: externalsecrets
    singular: externalsecret
  scope: Namespaced
  versions:
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
