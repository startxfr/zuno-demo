# TEST-ONLY FIXTURE. This is NOT the real OpenShift Route CRD - that
# ships cluster-wide with OpenShift itself and is never applied by this
# repo. It is a minimal, permissive stand-in so
# internal/controller/suite_test.go's envtest suite can exercise the
# reconciler's real Route-creation code path (a normal API create/update/
# get, identical to what happens against a real OpenShift API server)
# without needing a live OpenShift cluster. Never deploy this file for
# real - see operator/aiagent-operator/README.md.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: routes.route.openshift.io
spec:
  group: route.openshift.io
  names:
    kind: Route
    listKind: RouteList
    plural: routes
    singular: route
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
